# Decisions

ADR-lite log of non-obvious calls. Newest first. One entry per decision: **what · why · alternatives · affected**. Capture with `/compound decision`.

This is the highest-value handoff artifact: when ARM's dev team (or future-you) asks "why is it like this?", the answer lives here.

---

## Index

_Every decision, newest first — one row per heading, kept 1:1 (the `knowledge-guard` hook checks the count). When you append a decision, add its row here._

- **D-057** (07-22) — **Pre-handoff scope locks** (handoff-readiness review): responsiveness is **out of scope for all prototypes** — desktop-only, documented as a known limitation, not built (recategorizes the DEFERRED_DIRECTIONS responsive/a11y + career-map mobile-label items from "deferred pending v3" to won't-fix); `devSeedResults()` **stays in and gets documented** in the handoff (reverses HANDOFF_GUIDE §5's "delete before handoff"); ARM-blocked content (per-job/bridge copy, role-name confirm, AI-role variants, job CTA destination) becomes **placeholder-data notes in the handoff docs**, not folded-in project work. All three ecosystem repos verified synced to origin (robotics_career consolidated onto `main`); gates re-verified green
- **D-056** (07-20) — **Career-map chrome fix pass:** the persistent back button moves top-left and the desktop detail panel **stays left-docked** starting a space-2 gap below it (`PANEL_TOP_BELOW_BACK`); hub icons + the target star re-center via SVG `dominant-baseline="central"` (not `middle`, which sat them half a glyph-height high — L-013); the "Your career map" intro card trimmed to one line per sentence + widened 640→720 + `h3`→`h4` so it clears the hubs down to ~720px height. Panel-right was built then reverted (Caelan prefers left); camera-reserve rejected (fights the dismissible card's auto-collapse pan). Gates green; push is Caelan's
- **D-055** (07-18) — **CM-16: the Landing start CTA is a shared `PillButton` instance** (Dark Gold lg), not a new CtaButton variant and not a one-off: Kayla's 36px/20px-radius gold hero button already renders as the shipped PillButton pattern, so the Landing adopts the results gold-pill recipe; the `hero` Button variant + `--radius-cta`/`--spacing-cta-x` retired, CharacterSelect cards normalize to `rounded-lg`; REGISTRY.md amended (Landing CTA remaps CtaButton Gold → PillButton; Figma-side alignment queues with registry §6)
- **D-054** (07-18) — **CM-12: jobs stay panel-only on the career map** — the floating context panel carries the full three-tab job content and no standalone job page returns; the board's conditional ("build the full page only if usability sessions are still running") resolved to panel-only since no further sessions are scheduled — one crisp convention for the Fivestar handoff
- **D-053** (07-18) — **Kayla's career-map merge lands on `main`** after the three-pass review (`CAREER_MAP_REVIEW.md`, CM-01..19): the zoomable career map replaces the bubble map + job constellation + job-overview page as the results explore surface; the two new screens **`/character`** and **`/loading`** are accepted into the PRD (the hard-rule flags, resolved); the scenes gain the illustrated layer (backgrounds, character, thought bubbles); the orphaned `ResultsMap`/`BubbleField`/`bubbleLayout` chain + dead `breathe` bases deleted; specs reconciled (DATA_MODEL §17, PRD §5.0/§5.1, ARCHITECTURE, DESIGN_SYSTEM, CLAUDE.md, scene-motion skill)
- **D-052** (07-17) — **Componentization Pass 4 complete** — the robotics_career 4b tail + 4c Icon union executed (deferral pulled by Caelan): new shared `TopNav` code mirror of the DS mega set (`auth` in/out — out on auth screens + Explore, in on the dashboard-stub seam), `TopNavV2` reduced to Landing-OverHero-only, Icon union Path A (12 referenced names added → 63; all ~33 raw `material-icons` spans cleared). Closes that repo's FIGMA_MAP §5 divergences #1–#3 — **every enumerated Figma-leads-code divergence in the run is now closed (the contract note is satisfied)**. Its gates green (lint/typecheck/console-clean; that repo has no test suite by design) + focused `/design-review` PASS (2 p2 + 2 p3, all fixed in-session). Recorded in robotics_career as its D-011, committed `33e0ba5` on `refine/phase-2-loop` (push is Caelan's). Still open there, outside the run: button convergence (registry §6 "later") + the optional `@rc/ui` 61-name Icon bump. Only the conditional Pass 5 remains (later session, Caelan)
- **D-051** (07-17) — **Componentization Pass 4b, dashboard portion executed** (its gate settled): career_dashboard's Phase-5-mechanics branch verified merged to `main` + pushed with Track B unstarted, so the D-049 wait ended; Caelan confirmed **dashboard slice only**, on a fresh branch (`chore/componentization-4b-nav`, honoring that repo's don't-work-on-main note). Landed the D-043 one-liner (`CtaButton` `rounded-sm`→`rounded-md`, 6→8px) and **closed the TopNav item as a verified no-op** (the code already implements the winning AppHeader centered-search pattern; the registry's "actually centered" contrast was vs the retired dashboard *Figma* master, not the code — remaining diffs are per-repo token vocabulary per D-050/L-011, not chased). All dashboard gates green (lint/typecheck/163 unit/27 e2e) + focused `/design-review` PASS, no findings. Recorded in career_dashboard as its D-062. robotics_career 4b tail + 4c stay deferred; push/merge of the branch are Caelan's
- **D-050** (07-15) — **Code stays per-repo-aligned, not merged into one shared library, through the handoff.** The ecosystem unifies at the **Figma** design-system tier (the componentization run); the **code** tier stays per-repo local implementations *aligned* to that spec (Pass 4 = visual parity), NOT converted onto one shared `@rc/ui` component package. Reaffirms the ecosystem run's "consumer conversion = stretch-only" call (D-035 / `DESIGN_SYSTEM_RUN` §1) against Caelan's "should we just mirror everything?" — deferred because it has zero client-visible payoff (Fivestar rebuilds from the Figma + workflow docs), real pre-handoff regression risk on 3 green user-tested prototypes, and the repos genuinely diverge (even token vocabularies — the Pass-4a `text-label` friction). Post-handoff, if RC.org productionizes, converting all repos onto `@rc/ui` is the right move (the package already exists as the foundation).
- **D-049** (07-15) — Componentization **Pass 4 sliced** into per-concern sub-passes (small-passes principle): **4a** = explore_floor's 7 sanctioned normalizations (FIGMA_MAP §8 divergences #1–#7) + the two Flow Back buttons + delete orphaned `ProgressBar`/`SegmentedControl` — the **only slice run this cycle**; **4b** (nav: dashboard centered-search + CtaButton `rounded-sm`→`rounded-md`, robotics_career two auth states) and **4c** (Icon 61-name union) both **deferred**. Settled: Icon = **path A** (expand robotics_career's *local* `Icon.tsx`, clear its ~33 raw spans; the `@rc/ui` tag bump is parallel/optional — no blocking release round-trip); robotics_career = **light scope** (nav auth states only, button convergence deferred — come back to it); the **dashboard slice waits** until the career_dashboard Phase-5-mechanics branch settles (re-check with Caelan next session; note the name collision — that "Phase 5" ≠ this run's conditional "Pass 5"); CardHead/DEF-012 code side → folds into conditional Pass 5 if not already there; the two unpublished tokens (`--color-text-subtle`, `--color-white`) are **clear to publish** (no other active Figma-library work outstanding) pending a Caelan republish. Figma spot-check of the TopNav mega set + icon-slotted CtaButton/PillButton: **PASS** (as documented).
- **D-048** (07-08) — Componentization **Pass 3** (RC.org Captures file, complete): 10 logged-out nav instances swapped local `TopNavV2 State=Light` → shared dark `TopNav Auth=Out,Secondary=Off`, and 37 auth-form instances → the shared form family (FormField/OptionRow/OAuthButton/HintRow/StepFooter); 5 promoted local masters + the Light nav variant retired; Landing OverHero nav kept local (Caelan); every frame pixel-faithful except the sanctioned nav divergence (light→dark, "About us" dropped, search centered); `JobCard`/`SiteFooter`/`CompetencyTile`/`FilterDropdown`/`Chip`/brand icons stay local
- **D-047** (07-05) — Componentization **Pass 2b** (Interest Quiz tail, complete): results icon-pills/CTAs swapped to the icon-slotted shared PillButton/CtaButton (7 controls incl. Continue); 3 new local masters built (`SignalBar` Highlight axis, `RoleTab` State axis, `CompareTargetMenu` Target axis) + StatBox gained a `Size=Compact` variant; 20 instances swapped, all frames pixel-faithful; SignalBar omits an Integrator-active tone (never appears, ruling 9); QuestionCard/SceneCard/BridgeProgramRow + Flow Back buttons remain the deferred tail
- **D-046** (07-05) — Componentization: the PillButton/CtaButton icon-slot add becomes its own **Pass 1b** (shared-library amendment + republish, separate from the quiz-file Pass 2b); the compare dropdown (`CompareTargetMenu`) ruled a distinct element, not a PillButton
- **D-045** (07-05) — Componentization Pass 2 (partial): Interest Quiz `Components` page + 5 local masters (Chip/StatBox/ChoiceRow/TierBadge/HeroArrow) + shared TopNav ×9 (guest pill converges to Guest/"G") + CtaButton ×1 swapped, all 9 frames pixel-faithful; SignalBars / RoleTabs / cards deferred and the results icon-pills blocked on a PillButton/CtaButton icon-slot add → Pass 2b
- **D-044** (07-04) — Componentization Pass 1 shared tier built; TopNav model reshaped (reverses D-042 ruling 3): OverHero dropped, `Secondary` promoted to an independent axis (dashboard `In+On` / quiz `In+Off` / marketing `Out+Off`), search centered to nav width, secondary padding aligned, `Resources`/`Sign In` → List Row; dashboard boards swapped to `In+On`
- **D-043** (07-04) — CtaButton corner radius settles at 8px (`radius/md`, token-bound); the code ripple is one line (`CtaButton.tsx` `rounded-sm` → `rounded-md`, at Pass 4)
- **D-042** (07-03) — Componentization Pass 0 done: promotion registry ratified (`rc-design-system/REGISTRY.md`) — two button sets (CtaButton + PillButton), chip fold (MetaChip/GoldChip retire), nav OverHero kept as a Scroll axis, Icon union sized at 61 names
- **D-041** (07-03) — Figma componentization run ratified: nav mega set (centered search wins, Auth in/out), button standardization, chip consolidation, form-family promotion, Icon union in code, quiz tier local, no dark-mode restructure; run sheet = `COMPONENTIZATION_RUN.md`
- **D-040** (07-03) — Pass 7 remainder closed: extension-captured, variable-bound rebuilds replace the stills (502 paints bound); steps 7–8 done; capture pipeline = human extension click + MCP bind pass
- **D-039** (07-02) — Pass 7 executed: dark set published to the DS library, Interest Quiz file + FIGMA_MAP live, precedence flipped to the three-artifact model; captures are reference stills, editable rebuilds the remainder
- **D-038** (07-02) — Pass 7 adopts the dashboard's DS conventions wholesale: FIGMA_MAP manifest, post-publication precedence flip, keeps/proposals sections
- **D-037** (07-02) — Retire RC-CC as the token source: the `@theme` block is canonical, flow is code-outward (Pass 6 doc reconcile)
- **D-036** (07-02) — Control-height token ladder: dashboard's sm/md/lg adopted verbatim, xl/tap minted; remaining raw steps ratified
- **D-035** (07-02) — Ecosystem run GO: tokens-first package, multi-pass execution, asset rescue done
- **D-034** (07-01) — Adopt ARM Fivestar's four ideas + a domain-mirror path to the AI roles (documented, build deferred)
- **D-033** (07-01) — Warn-only knowledge-guard hook + snapshot/index conventions
- **D-032** (07-01) — Reorganize REMAINING_WORK into owner/timeframe docs (router)
- **D-031** (07-01) — Remove the dormant drag path (DragSortCard / DropZone)
- **D-030** (06-30) — Quiz-flow legibility pass (scenario-first, token-only sizes)
- **D-029** Phase F (06-30) — the job constellation + job overview
- **D-029** Phase E (06-30) — the ambient bubble map
- **D-029** Phase C (06-29) — the dark results role-cards screen
- **D-029** (06-26) — Step 8 = the dark visual re-architecture (plan + locked calls)
- **D-028** (06-25) — Realignment Phase 5: collapse four categories to three roles
- **D-027** (06-25) — Realignment Phase 4: strip to the narrative flow
- **D-026** (06-25) — Realignment Phase 3: re-point harness gates at §17 invariants
- **D-025** (06-25) — Realignment Phase 2: re-center the spec set (documented-cut convention)
- **D-024** (06-25) — Kit-align explore_floor's design tokens to the RC UI Kit
- **D-023** (06-22) — Narrative intro questions (education + salary) nudge the match
- **D-022** (06-11) — Role select arms on the switcher; the CTA launches it
- **D-021** (06-11) — Classic leaves the switcher; role-select takes its slot; narrative default
- **D-020** (06-08) — Screeners drive an always-on education/pay fit line on results
- **D-019** (06-08) — Exam screener questions (Q1/Q2) nudge the category match
- **D-018** (06-08) — Narrative scenes are a per-choice 3-bucket sort; middle "Kinda me"
- **D-017** (06-07) — Question-structure study: three flows, four categories, per-flow results
- **D-016** (06-04) — A/B question-set instrument between Phase 1 and Phase 2
- **D-015** (05-30) — Phase 1 sort is drag + tap only (no arrow-key/Enter mechanic)
- **D-014** (05-30) — Phase 2 sort model: drag parts off the belt (superseded-in-practice)
- **D-013** (05-29) — Tailwind v4 (CSS-first): tokens live in `@theme`
- **D-012** (05-29) — Don't ship a project `.mcp.json`; configure MCP globally
- **D-011** (05-29) — Defer the live RC.org capture (superseded-in-practice)
- **D-010** (05-29) — Motion skills via the free MIT community package
- **D-009** (05-29) — Declare project MCP servers in `.mcp.json` (superseded by D-012)
- **D-008** (05-29) — Knowledge lives in-repo; mirror durable facts to built-in memory
- **D-007** (05-29) — Build a bespoke harness; don't install the BilLogic plugin
- **D-006** (05-29) — Allow demo affordances (superseded-in-practice)
- **D-005** (05-29) — Remove project timeframes from the docs
- **D-004** (05-29) — Landing scene gets a Phase 1 GSAP `DrawSVG` reveal
- **D-003** (05-29) — Vitest is part of the stack and `pnpm test`
- **D-002** (05-29) — Standardize the file-size re-check threshold at ~250 lines
- **D-001** (05-29) — Innovator archetype max = 27; scoring normalizes per archetype

---

## 2026-07-22

### D-057 — Pre-handoff scope locks: desktop-only, keep+document devSeedResults, ARM content stays placeholder

- **Decision:** Three scope calls made during the handoff-readiness review, all locked for the client handoff. (1) **Responsiveness is out of scope for every prototype** (explore_floor, career_dashboard, robotics_career) — the experiences target desktop (DESIGN_SYSTEM §5), and mobile/responsive fit is documented as a known limitation rather than built. (2) **`devSeedResults()` stays in the code** (`sessionStore.ts` type decl + impl + the Landing prose comment) and is **documented in the handoff** as a dev/demo affordance, reversing HANDOFF_GUIDE §5's "delete all three before handoff." (3) **ARM-blocked content** — per-job/bridge-program copy (the `⚠️ PLACEHOLDER` files), the three-role-name confirmation vs Fivestar, the AI-prefixed role variants, and the job "next steps" CTA destination — becomes **notes in the handoff documentation** ("this is placeholder data / an ARM decision, replace it") rather than work folded into the prototype.
- **Why:** We're past the 07-21 presentation and prepping the final handoff. These items either belong to ARM/Fivestar (content, taxonomy) or are deliberate prototype affordances the receiving dev team benefits from knowing about. Documenting beats building: Fivestar rebuilds from the Figma + workflow docs, so investing prototype effort in mobile fit or ripping out a harmless demo seed has no client payoff and adds regression risk to three green, user-tested prototypes.
- **Alternatives:** Build a responsive story + 44px touch targets (rejected — no payoff for a desktop-target prototype handed to a rebuilding team; was the "medium priority handoff artifact" framing in DEFERRED_DIRECTIONS, now explicitly dropped). Delete `devSeedResults()` per the original plan (rejected — Caelan wants it kept and documented; it's already dead in production builds via the `import.meta.env.DEV` guard removal). Fold ARM content/decisions into the prototype (rejected — not ours to author or decide; the data-request template already carries the ask).
- **Affected:** `DEFERRED_DIRECTIONS.md` (responsive/a11y + career-map mobile-label sections recategorized won't-fix/documented desktop-only); `HANDOFF_GUIDE.md` (its §5 devSeedResults instruction and §1–§4 ARM items become "document, don't do" — folded into the deferred rewrite, held until wrap-up work finalizes); `STATUS.md` (open-items + push line). No code change. The career-map mobile-label overflow (the one open p2 from the merge review) is subsumed here as a documented desktop-only limitation.

## 2026-07-20

### D-056 — Career-map chrome: back button top-left, detail panel stays left below it

- **Decision:** Post-merge fix pass on the landed career map (three issues Caelan flagged from screenshots). (1) **Chrome layout:** the persistent "Back to your results" exit (CM-09) moves from top-right to **top-left**; the desktop detail panel (CM-10/11) **stays left-docked** and now starts a `space-2` (12px) gap **below** the back button (`PANEL_TOP_BELOW_BACK = 16 + 36 + 12` in `CareerMap.tsx`, i.e. `top-space-3` + `h-control-lg` + gap), so the exit and the panel share the top-left corner without overlapping. (2) **Hub icons + target star** center via SVG `dominant-baseline="central"`, not `"middle"` (see L-013). (3) **Intro card** ("Your career map") trimmed to one line per sentence, `--container-map-card` widened 640→720, title `h3`→`h4`, divider/padding tightened — height 269→165px so it clears the hubs from ~720px viewport height up.
- **Why:** Back-button-top-left and panel-on-the-left are both Caelan's explicit calls. The icons rendered half a glyph-height high (the `middle`-baseline bug). The card overflowed onto the hubs because the overview camera packs the cluster higher on shorter viewports while the old 3-sentence copy wrapped to ~6 lines — so shrinking the card alone wasn't enough; the copy had to drop to one line each.
- **Alternatives:** **Panel docks right** to free the top-left corner cleanly — built and verified first, then reverted at Caelan's request (he prefers the panel left; the `'right'` `MapPaneDock` was removed, not left as dead code). **Camera reserves top space** for the card (rejected: the intro card is dismissible and auto-collapses the moment the user pans/zooms, so a camera-tied top reserve would fight the user's own pan on collapse, and a naive downward y-bias gets undone by `careerMapClampCamera`).
- **Affected:** `CareerMap.tsx` (back button `right`→`left`, panel `left` + `PANEL_TOP_BELOW_BACK` top, intro-card compaction), `CareerMapHubLabel.tsx` (icon baseline), `data/flows/narrativeFlow.ts` (map copy), `styles/globals.css` (`--container-map-card`). Gates green: lint, typecheck, 102 unit, 4 E2E. Committed on `main`; push pending (Caelan's).

## 2026-07-18

### D-055 — CM-16: the Landing start CTA is a shared PillButton instance

- **Decision:** Rule Kayla's `hero` Button variant (the redesigned Landing's start CTA, Figma 1368:450 — 36px tall, 20px radius, gold, 30px padding) a **shared `PillButton Surface=Dark Fill=Gold Size=lg` instance**. The Landing adopts the results gold-pill recipe verbatim (`h-control-lg rounded-full bg-arm-gold px-space-4 text-body font-medium`, the same classes as the results control-bar pill); the `hero` variant is deleted from `Button.tsx` and its two one-off tokens (`--radius-cta` 20px, `--spacing-cta-x` 30px) retired. CharacterSelect's cards, the radius token's only other consumer, normalize to `rounded-lg` (16px, the established dark-card surface step — imperceptible on a 478×549 card). `rc-design-system/REGISTRY.md` carries the amendment: the Landing CTA remaps from `CtaButton Gold Filled lg` to PillButton; Figma-side alignment of the redesigned landing frame queues with the registry §6 convergence tail.
- **Why:** At 36px height a 20px radius already renders as a full pill, so the button *is* the shipped PillButton Dark Gold pattern — the same recipe already instanced in `ResultsExperience`. Minting a CtaButton pill variant would duplicate the PillButton set; keeping the one-off would leave a fourth button vocabulary and two orphan tokens. Kayla's design intent (a gold pill hero CTA) is preserved; this is normalization, not reversal.
- **Alternatives:** a new `CtaButton` hero variant (duplicates PillButton); keep as a logged one-off (leaves the drift the componentization run exists to prevent).
- **Affected:** `Landing.tsx`, `Button.tsx`, `CharacterSelect.tsx`, `globals.css`, `rc-design-system/REGISTRY.md`, `PRD.md` §5.1.

### D-054 — CM-12: jobs stay panel-only on the career map

- **Decision:** The career map's job zoom is the **final job surface** — the floating `MapContextPanel` carries the full three-tab job content (overview / skills & competencies / "How you fit" with the trajectory ladder), and no standalone job-overview page returns. The pre-merge "Role overview → / Job overview →" footer CTAs are dropped.
- **Why:** The board's CM-12 ruling was conditional on testing: build the full page and measure only if job-seeker usability sessions were still running when Pass 2 landed. Caelan confirmed (2026-07-18) no further sessions are scheduled, so the conditional resolves to panel-only — one crisp convention (roles get full-bleed cards, jobs live on the map) for the Fivestar handoff, with no rebuilt page that would never get validated.
- **Alternatives:** rebuild the full job page behind an expand affordance (unvalidatable now, and it re-introduces the convention break CM-12 flagged).
- **Affected:** `MapContextPanel`/`MapPanelJob`, `useResultsNav` (no `job-overview` view), `DATA_MODEL.md` §17, `PRD.md` §5.0, `CAREER_MAP_REVIEW.md` CM-12.

### D-053 — Kayla's career-map merge lands on main

- **Decision:** Merge `career-map-merge` (Kayla's `visuals` + `career-map` branches, reconciled and reviewed) to `main`, closing the `CAREER_MAP_REVIEW.md` ledger (CM-01..19 all landed, decided, or dispositioned). The zoomable **career map** replaces the bubble map + job constellation + job-overview page as the results explore surface. Two new screens are **accepted into the PRD** (resolving the new-screen hard-rule flags): **`/character`** (pick who you play as, between landing and quiz) and **`/loading`** (the tabulating interstitial before results). The scenes gain the illustrated layer (backgrounds, character, thought bubbles — all mapped in data), the quiz surfaces sit on the raised panel glass (CM-01), and the Landing carries the factory-floor hero. Cleanup rode along: the orphaned `ResultsMap`/`BubbleField`/`bubbleLayout` chain, the dead `breathe.bubble/node/sparkle` bases, and the `--container-map` token deleted.
- **Why:** The map concept and visual direction are an upgrade (the constellation motif carried no meaning; the map gives the explore surface a spatial story and seeds outcome tracking via the target-role marker), and the three-pass review fixed the execution regressions inside her framework rather than rolling them back. All gates green at every pass (final: lint zero-warning, typecheck, 102 unit, 4/4 E2E) plus a rubric design-review pass.
- **Alternatives:** roll back to the constellation architecture (rejected at review start — see the ledger's verdict); merge unreviewed (the ledger existed precisely because several silent regressions needed fixing first).
- **Affected:** the whole results explore surface, the flow's visual layer, `/character` + `/loading` routes, the spec set (DATA_MODEL §17, PRD §5.0/§5.1, ARCHITECTURE, DESIGN_SYSTEM, CLAUDE.md, the scene-motion skill), `STATUS.md`, the componentization registry (D-055).

## 2026-07-17

### D-052 — Componentization Pass 4 complete: robotics_career 4b tail + 4c Icon union executed

- **Decision:** Pull the robotics_career deferral and finish Pass 4 in one session (Caelan: "take on 4b and 4c so we can close this out"; Pass 5 ruled to a later session). **4b:** a new `robotics_career/src/shell/TopNav.tsx` — the code mirror of the DS `TopNav` mega set (`636:40`), built from the master's real geometry (`get_design_context` on the `Auth=Out` variant `632:38`): near-black 60px bar, wordmark lockup (`rc_logo_white_text.png` copied into that repo's `public/`, sibling convention), centered 440px scoped search (always visible; submit → `/explore?q=`), right slot `Resources ⌄ · divider · Sign In · gold Sign Up` (CtaButton-style, inline — that repo's button convergence stays registry-§6 "later") for `auth="out"`, glass profile pill (gold "J" + "Jordan", the DS master persona) for `auth="in"`. Mounted out on AuthLayout + Explore, in on the DashboardStub seam; sticky in flow (clearance paddings removed); `TopNavV2` trimmed to Landing-OverHero-only, the Landing untouched. **4c (Path A per D-049):** that repo's local `Icon.tsx` gained exactly the 12 names its raw spans referenced (→ 63 names; `chevron-u`=`expand_less` matching this repo's naming) and all ~33 raw spans cleared — `grep material-icons src` hits only the atom.
- **Why:** These were the run's last open code items; landing them closes robotics_career's FIGMA_MAP §5 divergences #1–#3, and with 4a (§8 #1–#7) + the dashboard radius already closed, **no Figma-leads-code divergence survives the run** — the contract note is satisfied and Pass 4 completes. The `@rc/ui` 61-name bump stays optional/parallel (that repo imports its local Icon; D-049 said no blocking release round-trip). Design-review findings folded in before closing: nav fills on the `on-dark-*` family (the tokens the package minted for the dark nav — also what the dashboard's own TopNav uses), `text-ink` on gold (text role, not the `near-black` surface role), space-token spacing, stub mobile padding. Deliberate keep, documented in its D-011: nav text rides `text-on-dark`/`-faint` (the exact Figma-master bindings) rather than the dashboard's `on-dark`/`-muted` — per-repo vocabulary per D-050/L-011.
- **Alternatives:** keep the deferral (rejected — Caelan explicitly pulled it); reskin `TopNavV2` in place with modes (rejected — one component carrying a retired design plus the live one; the mega set's model is one bar, two auth states, with OverHero file-local per D-044/D-048); full 61-name union now (rejected — Path A adds only what's referenced; the superset lands with the optional package bump).
- **Affected:** robotics_career `src/shell/{TopNav,TopNavV2,index}`, `src/screens/{Explore,DashboardStub}`, `src/screens/auth/{AuthLayout,SignUp,SignIn,SetupLocation,shared}`, `src/components/Icon.tsx`, `public/rc_logo_white_text.png`, its `docs/figma/FIGMA_MAP.md` §5 + D-011 + STATUS + session note; committed **`33e0ba5`** on `refine/phase-2-loop` (**push is Caelan's** — the branch is now 4 ahead). This repo: `COMPONENTIZATION_RUN.md` (Pass 4 ☑ + outcomes), `STATUS.md`. Run state: **Passes 0–4 complete; only conditional Pass 5 remains.**

### D-051 — Componentization Pass 4b, dashboard portion executed; TopNav closed as a verified no-op

- **Decision:** Run the deferred 4b **dashboard portion now** — its D-049 gate (career_dashboard's Phase-5-mechanics branch settling) was verified closed by direct repo inspection: `p5/01-mechanics-reset` fully merged to `main` (both at `6cb2c56`), `main` even with `origin/main` (already pushed), working tree clean, Track B (its next workstream) unstarted. Caelan confirmed go, **dashboard slice only** (robotics_career 4b tail + 4c stay deferred), on a **fresh branch** `chore/componentization-4b-nav` per that repo's don't-work-on-main note. Two items: (1) the D-043 radius one-liner **landed** (`CtaButton.tsx` `rounded-sm`→`rounded-md`, 6→8px — a class change, not a `--radius-sm` token change, so the sanctioned 6px inputs/chips/pills family is untouched); (2) "TopNav → centered search" **closed as a verified no-op** — the dashboard's `TopNav.tsx` already implements the winning centered-search design (this repo's `AppHeader`, D-041): flex row of logo · `mx-auto w-full max-w-[440px]` search · profile.
- **Why:** The gate existed to avoid editing the dashboard's nav mid-flight; with the whole Phase-5 code pass merged, pushed, and its Track B not yet begun, there was nothing to collide with. The TopNav no-op is recorded explicitly (rather than silently skipped) so the run's come-back flag closes with evidence: the registry's "search actually centered" contrast was against the **retired dashboard Figma master** (`262:30`), never this code, and the ~50px remaining-space bias vs true bar-center is inherent to the winning AppHeader design itself (imperceptible; design review confirmed). Remaining TopNav diffs are per-repo token vocabulary (`on-dark-*` vs `glass-*`), which D-050/L-011 say not to chase.
- **Alternatives:** work on the dashboard's `main` directly (rejected — its STATUS forbids it for the next code work); re-center the search to true bar-center (rejected — the spec-winning implementation centers in remaining space; matching Figma's frame-centering would need absolute positioning for no visible gain); include the robotics_career 4b tail while at it (rejected — Caelan scoped this session dashboard-only; robotics_career stays deprioritized).
- **Affected:** career_dashboard `src/components/CtaButton.tsx` (+ its D-062 / STATUS / session note `2026-07-17-componentization-4b-radius.md`; gates green — lint/typecheck/163 unit/27 e2e; focused `/design-review` PASS, no findings). This repo: `COMPONENTIZATION_RUN.md` (pass table, slice map, come-back flag closed, 4b outcomes), `STATUS.md`. **Push + merge of the dashboard branch are Caelan's.** Pass 4's remainder = the robotics_career 4b tail + 4c.

## 2026-07-15

### D-050 — Code stays per-repo-aligned, not merged into one shared library, through the handoff

- **Decision:** The RC.org ecosystem unifies at the **Figma design-system tier** (the componentization run's shared component library, all three consumer files read from it). The **code tier does not** merge into one shared `@rc/ui` component package before the late-July handoff — each repo keeps its own local component implementations, *aligned* to the unified spec (Pass 4 is visual/dimensional parity, not a package migration). This reaffirms the ecosystem run's "consumer conversion (step 5) = stretch-only" call (`DESIGN_SYSTEM_RUN.md` §1, D-035), raised again by Caelan's "should we not just mirror everything between the repos?"
- **Why:** Three reasons hold until handoff. (1) **Zero client-visible payoff** — Fivestar's dev team rebuilds from the Figma mockups + workflow descriptions, not our prototype source; a shared code library across our throwaway prototypes changes nothing they receive. (2) **Real regression risk** — converting three green, user-tested prototypes onto one package (deleting local token authorship, rewiring imports) weeks before the deliverable is how a working demo breaks. (3) **The repos genuinely diverge** — dark narrative quiz vs. data dashboard vs. marketing site, with different token vocabularies; the Pass-4a `text-label` friction (a token that exists in the dashboard theme but not explore_floor's, silently dropped by Tailwind v4) is that divergence in miniature, and full code-sharing would surface many such frictions mid-crunch.
- **Alternatives:** full `@rc/ui` consumer conversion now (rejected — pre-handoff risk, no client payoff; it's the documented stretch, viable post-handoff); bespoke per-repo with no alignment at all (rejected — the componentization run exists precisely so the handoff files read as one system).
- **Affected:** No code change — a strategic confirmation. Governs Pass 4's shape (align, don't merge) and the answer of record to "why isn't there one shared code component library." **Post-handoff:** if RC.org productionizes this, converting every repo onto `@rc/ui`'s shared components is the right next step, and `@rc/ui` already exists as the foundation (D-035), so it's a clean migration, not a rebuild.

### D-049 — Componentization Pass 4 sliced into per-concern sub-passes; the code-alignment plan of record

- **Decision:** Pass 4 (code alignment — the "hops repos" pass that closes every sanctioned Figma-leads-code divergence) is **split into three per-concern slices** so each is a single-session unit with one repo's gates (the small-passes principle, `feedback-small-session-passes`). Only **4a** runs this cycle:
  - **4a — explore_floor** (self-contained, explore_floor gates only): the 7 sanctioned normalizations catalogued in `FIGMA_MAP.md` §8 "Sanctioned Figma-leads-code divergences" — (1) guest pill → initial avatar in `AppHeader.tsx`, (2) Landing CTA 149×46 → CtaButton lg 120×36 r8, (3) Compare Education StatBox, (4) Continue CTA → 120×36, (5) Role-overview CTA → CtaButton Teal lg (keep full-width + 54h), (6) Set-as-target pill → PillButton Dark/Gold (`px-space-4`→`px-space-3`), (7) RoleTab inactive underline (already renders identically) — plus reconciling the two **Flow Back buttons** (`25:188`/`25:249`, the §8 deferred-tail note) and **deleting the orphaned `ProgressBar` + `SegmentedControl`** (zero call sites, Caelan: "get rid of the orphaned stuff").
  - **4b — nav code (DEFERRED):** career_dashboard `TopNav` centered-search (verify — the code already uses `mx-auto`, so this may already be aligned) + `CtaButton` `rounded-sm`→`rounded-md` (D-043); robotics_career's two nav auth states (light 80px → dark 60px mega nav per §5 divergence #1).
  - **4c — Icon union (DEFERRED, robotics_career):** **path A** — expand robotics_career's **local** `src/components/Icon.tsx` to the names its raw spans need (the union superset is 61; add what's used), then clear its ~33 raw `material-icons` spans (24 in `Explore.tsx`). The `@rc/ui` package bump to the 61-name union is a **parallel, optional** step, not a blocker for clearing the spans.
- **Why:** Pass 4 spans four repos with independent gates; a single monolithic pass would be un-reviewable and would couple explore_floor's clean local work to cross-repo release friction. **Path A (Icon)** was chosen because robotics_career imports its *local* `Icon`, not `@rc/ui`'s — so landing the union only in `@rc/ui` would not give its code the glyphs, and a package-only route also drags in a Caelan-only tag-push round-trip (`@rc/ui` is pinned `github:caelar/rc-design-system#v1`; rc-design-system `main` is itself unpushed). Expanding the local Icon keeps 4c self-contained. **robotics_career is deprioritized** ("don't care right now") so 4b/4c are documented and parked, with the button *convergence* explicitly out of scope (light scope) — come back to it. **The dashboard slice waits** until career_dashboard's active Phase-5-mechanics branch (`p5/01-mechanics-reset`, D-053–D-058, unpushed) settles, to avoid editing its nav mid-flight; re-check with Caelan next session. **Two-token publish:** Caelan reopened the Pass-2 "keep, no publish" call, conditional on no other Figma work pending — verified none is active (componentization is code-only from here bar the conditional Pass 5; the ecosystem run is done through Pass 7), so publishing `#595959` as a library primitive + confirming `--color-white` is clear, but it is a shared-library write + a Caelan republish, so it's staged as a recommendation, not executed.
- **Alternatives:** run Pass 4 as one pass (rejected — cross-repo, un-reviewable, couples clean work to release friction); Icon **path B** (migrate robotics_career to import `@rc/ui`'s Icon, bump+tag the package) — rejected as the near-term route (bigger diff + Caelan-only tag push), viable later as part of the `@rc/ui` consumer-conversion stretch; do the dashboard nav slice now (rejected — collides with the live Phase-5 branch); converge robotics_career's buttons now (rejected — Caelan set light scope); publish the two tokens unilaterally (rejected — shared-library write reversing a settled call, and the republish is Caelan's).
- **Affected:** `COMPONENTIZATION_RUN.md` (Pass 4 row + a "Pass 4 slice map" section), `STATUS.md` (Next up + Awaiting-Caelan). No code touched by this decision — 4a execution + its session note follow. Come-back flags recorded: robotics_career (4b nav + 4c icons + deferred button convergence), the dashboard nav slice (gated on its Phase-5 settling; re-check next session), CardHead/DEF-012 → conditional Pass 5, and the two-token republish (Caelan).

## 2026-07-08

### D-048 — Componentization Pass 3: RC.org Captures file swapped to shared library instances
- **Decision:** Pass 3 re-points the RC.org Captures file (`F3GRK7HNLLtG48vPosyXKw`, robotics_career) local-master instances to DS-library instances where a shared component exists. **Nav (10):** every logged-out `TopNavV2 State=Light` → shared `TopNav Auth=Out, Secondary=Off` (variant `0b0673a3…`, set `f69105be…`) — 8 auth screens + 2 Explore frames. **Forms (37):** FormField ×10 (`88dd923c…`), OptionRow ×12 (set `d31a14f7…`; Checkbox/Unchecked `d5a94ce6…` ×9, Radio/Unchecked `c2c192df…` ×3), OAuthButton ×4 (`431abdb5…`), HintRow ×5 (`5067f0a9…`), StepFooter ×6 (`b3c1d94b…`). `swapComponent` preserved every override (labels, dropdown chevrons, password-eye glyphs, the Google brand icon) and mapped them into the DS component properties, so no manual re-set was needed (the DS masters were promoted from these exact captures, L-009). Retired the 5 now-orphaned local form masters + the `TopNavV2 State=Light` variant (0 instances doc-wide); kept the set for OverHero. All 47 swaps verified pixel-faithful except the intended nav change.
- **Why:** Pass 3 is the run's third core pass — make the RC.org handoff screens read from the same design system as the dashboard and quiz. It is a sanctioned "Figma-leads-code" inversion (confined to Passes 1/3, closed by Pass 4): the file adopts the shared components even where they diverge from Kayla's current code. **Landing OverHero nav kept local (Caelan, this session):** the mega set dropped its OverHero variant at D-044, so the Landing hero's transparent-over-hero nav has no shared equivalent — it stays a Captures-local master (`TopNavV2`, now OverHero-only). The 80→60px nav height was absorbed by auto-layout on the 8 auth frames and by a manual 20px content nudge (+ frame −20px) on the 2 absolute Explore frames, preserving the original nav-to-content gap (Caelan chose "tighten gap").
- **Alternatives:** swap the Landing OverHero nav to solid Out+Off (rejected — loses the marketing look, no like-for-like target); restore an OverHero variant to the shared set (rejected/deferred — reopens D-044 for no Pass-3 payoff); hold the auth screens on the light local nav (rejected — the whole point is nav unification); leave the local form masters in place as a fallback (rejected — they are exact duplicates of published DS components, recoverable by key, so deletion is lossless and reads cleaner for handoff). New sanctioned Figma-leads-code divergences (close at Pass 4): (1) nav light→dark unification (local light 80px, left search, `Resources ▾ · About us · Sign up · Sign in` → shared dark 60px, centered search + sliders, gear+wordmark, `Resources ⌄ · Sign In · gold Sign Up`; **"About us" dropped**, casing changed); (2) Explore nav search now visible (local hid it; the mega set always shows the centered search); (3) content nudged up 20px to compensate the shorter nav. Code (`TopNavV2.tsx`, auth screens, `Explore.tsx`) still renders the light nav / hidden search.
- **Affected:** `robotics_career/docs/figma/FIGMA_MAP.md` §5 (local tier updated + a Pass-3 swap/divergence subsection), `COMPONENTIZATION_RUN.md` (Pass 3 ticked + outcomes), `STATUS.md`. Next core pass: **Pass 4 — code alignment** (nav auth states, button standardization, Icon 61-name union; hops repos). Passes 0–3 done; only Pass 4 (+ conditional Pass 5) remain.

## 2026-07-04

### D-047 — Componentization Pass 2b: the Interest Quiz deferred tail closed (icon-pill swaps + 3 net-new local masters)

- **Decision:** Pass 2b closes the D-045 deferred tail in the Interest Quiz file (`pjgrRJS5YYII1iciW7Pak2`), all local (ruling 7), no republish. (1) **Results icon-pills/CTAs** swapped to the Pass-1b icon-slotted shared sets — `PillButton Dark/Outline` (Compare roles / Skip to map / Back to Technician / Back to Technician careers), `PillButton Dark/Gold` (Set as target role), `CtaButton Filled/Teal/lg` (Role overview, full-width preserved), `CtaButton Filled/Gold/lg` (Continue) — glyphs per the run-sheet map. (2) **Three net-new local masters** built by L-009 clone-of-bound-occurrence: **`SignalBar`** (`127:175`, `Highlight`{Technician,Specialist,Off} + Label/Value, responsive track, per-instance fill-px), **`RoleTab`** (`131:238`, `State`{Active,Inactive} + Label, serves both the 2-tab and 3-tab strips), **`CompareTargetMenu`** (`134:263`, `Target`{Technician,Specialist,Integrator} swatch + Label/TargetName, the D-046 carve-out). (3) **StatBox** promoted to a set (`117:7`) with a **`Size`**{Default,Compact} axis; the 2 JobSidePanel compacts swapped. 20 instances total; every affected frame re-verified pixel-faithful; all masters token-clean.
- **Why:** Pass 1b (D-046) published the icon slots, unblocking the pill/CTA swaps — the highest-value tail item, so it ran first (Caelan's sequencing). The three masters were the variant-heavy work Pass 2 deferred as "lossy as-is"; extracting each from a token-bound occurrence (L-009) carried the bound paints over so no re-binding was needed for the shipped tones. **SignalBar omits an Integrator-active variant** — Integrator is never the top/target role in the captured frames, so per ruling 9 (variant only for states that ship) the Highlight axis is Technician/Specialist/Off only. The `#595959` inactive SignalBar fill stays raw (the §6 deliberate keep — no library primitive).
- **Alternatives:** leave SignalBars/RoleTabs as faithful frames (rejected — they are real repeated components; the run promotes them); make the CompareTargetMenu a PillButton (rejected at D-046 — wrong shape); build a full 3-role SignalBar Highlight incl. Integrator (rejected — speculative, ruling 9). New sanctioned Figma-leads-code normalizations (close at Pass 4): Continue 139×46 → CtaButton lg 120×36 (parallels Landing); Role overview bespoke 354×54 → CtaButton Filled/Teal/lg kept full-width (FILL) + the panel's 54px footprint (instance height override; standard lg is 36); Set-as-target gold span → PillButton Dark/Gold (`px-space-4`→`px-space-3`, 15px narrower). StatBox Default made responsive (value paragraph/text FILL, was a hardcoded FIXED 322) — a structural repair surfaced by the combine.
- **Affected:** `FIGMA_MAP.md` §8 (master + instance registry), `COMPONENTIZATION_RUN.md` (Pass 2 → done, Pass 2b outcomes), `STATUS.md`. Closes the D-045/D-046 tail except QuestionCard (`25:96`) / SceneCard (`25:149/224`) / BridgeProgramRow (not captured) and the two Flow **Back** buttons (`25:188/25:249`, not in the glyph map) — the remaining tail. Next core pass: **Pass 3 — Captures file**.

### D-046 — Componentization: icon-slot add is Pass 1b (a shared-tier amendment); compare dropdown is a distinct element

- **Decision:** The `PillButton`/`CtaButton` icon-slot add (needed to unblock the results icon-pills, D-045) becomes its own **Pass 1b** — a shared-library amendment run as a separate session, **not** folded into the quiz-file Pass 2b. Additive leading + trailing glyph slots (`Show Icon` booleans default off), token-bound per variant; handoff spec in `COMPONENTIZATION_RUN.md`. Separately, the compare **"Compare with ● Specialist ⌄" dropdown** (`CompareTargetMenu.tsx`) is ruled a **distinct element, not a PillButton** — different shape/size/behavior — so it stays a quiz-local master built in Pass 2b, excluded from the icon work.
- **Why:** The icon slot edits the DS library, which the dashboard + robotics_career also consume — ecosystem blast radius + a republish dependency. Folding that into a quiz-file pass would break the run's one-surface-per-pass discipline and re-gate Pass 2b on Caelan. Keeping 1b separate lets Pass 2b's local work (SignalBars / RoleTabs / cards / compact variants) run unblocked now, with the icon-pill/CTA swaps folding in after the republish. The dropdown's shape/size/behavior differ enough that forcing it onto the pill set would be unfaithful.
- **Alternatives:** roll the icon slot into 2b (rejected — hides an ecosystem-wide library change inside a file pass and re-gates 2b on the republish); make the dropdown a PillButton variant (rejected — wrong shape/behavior, it's a select).
- **Affected:** `COMPONENTIZATION_RUN.md` (Pass 1b ledger row + handoff spec), `FIGMA_MAP.md` §8 (dropdown carve-out), the future Pass-1b session (library) + Pass-2b session (quiz). Refines D-045.

### D-045 — Componentization Pass 2 (partial): Interest Quiz file componentized; guest-nav converges; icon-slot blocker surfaced

- **Decision:** Pass 2 turned the 9 Interest Quiz frames (`pjgrRJS5YYII1iciW7Pak2`) into instances — **partially**, prioritizing the clean, most-repeated elements and honestly deferring a variant-heavy / icon-blocked tail. Built a `Components` page (`84:96`) with **5 local masters** (Chip `84:99`, StatBox `87:105`, ChoiceRow `90:80`, TierBadge `94:134`, HeroArrow `94:139`, extracted by cloning token-bound occurrences) and swapped **Chip ×4 / StatBox ×8 / ChoiceRow ×5 / TierBadge ×1 / HeroArrow ×2**, plus shared **TopNav ×9** and **CtaButton ×1**. Manifest: `FIGMA_MAP.md` §8. Three sanctioned Figma-leads-code divergences (close at Pass 4): guest nav pill converges to the shared `ProfileMenu` (initial "G" vs `AppHeader`'s person-icon; **Caelan-approved** over fixing the master or keeping a local header); Landing CTA normalized to CtaButton lg 120×36; compare-Education StatBox body normalized (bulleted `<ul>` → Value override, muted-bound). Token call: **keep** (#595959 raw, whites aliased to On CTA; no new unbound hex).
- **Why:** Local components stay local (D-041 ruling 7), so Pass 2 needs no library publish and nothing from Caelan; the payoff is the handoff file reading as a real design system. Partial-over-broken: several results elements have real structural nuance (SignalBars = responsive width + per-panel highlight + fixed fills) or are **blocked** — `PillButton` and `CtaButton` are label-only, so every icon-bearing results pill/CTA can't swap without losing its icon. That blocker is a **Pass-1 library change** (add an icon slot), not a Pass-2 workaround, so those swaps move to Pass 2b alongside the fix. Nothing was destroyed for the deferred items — they remain faithful original frames.
- **Alternatives:** (a) force the icon pills onto the label-only sets — rejected (drops icons, unfaithful); (b) fix the shared TopNav master for the guest pill — offered to Caelan, he chose convergence; (c) build every registry §4 master this session — rejected for budget/edge-case risk (the compare-Education flatten showed the cost); land a documented partial instead.
- **Affected:** Interest Quiz Figma file (`Components` page + 9 frames now instanced); `FIGMA_MAP.md` §8; `COMPONENTIZATION_RUN.md` Pass 2 (◑, Pass 2b defined). Code untouched (Pass 4 owns the 3 reconciles). Pass 2b: the deferred tail + the PillButton/CtaButton icon-slot add.

### D-044 — Componentization Pass 1: shared tier built; the TopNav model is reshaped at build time
- **Decision:** Pass 1's shared tier is built in the DS library — the **TopNav mega set** (`636:40`), **PillButton** (`686:2`), and the **form family** (`Forms` page) — and the **TopNav model diverges from the D-042 spec** on Caelan's live direction: (1) **OverHero dropped** (the `Scroll` axis is gone); (2) **`Secondary` is an independent variant axis, not tied to Auth** — the secondary nav is dashboard-only, so `Auth`{In,Out} × `Secondary`{On,Off} with 3 shipped combos (`In+On` dashboard, `In+Off` quiz, `Out+Off` marketing); (3) the published `SecondaryNav` (`260:2`) **nests as an instance** inside `In+On` rather than being duplicated or bundled as markup; (4) the search is **centered to true nav width** (equal FILL columns wrap the logo and right slot); (5) the nested secondary's **edge padding = 16px** to match the top bar; (6) `Resources`/`Sign In` use the **List Row** style. The 3 dashboard boards were swapped to `In+On` (each board's separate SecondaryNav deleted).
- **Why:** Caelan on OverHero: "makes little to no sense in the current prototype, and after revision it will make none" — a single unused marketing state didn't earn an axis. On Secondary: a logged-in *quiz* user (the Guest pill) has no secondary, so presence tracks the surface, not auth — an independent axis models the three real surfaces exactly, and nesting the existing SecondaryNav keeps one source of truth. Centering and padding were legibility/alignment fixes he called out directly. The dashboard swap to `In+On` (vs keeping a separate SecondaryNav) realizes the "dashboard uses the variant" intent and verified clean (active pill + counts intact).
- **Alternatives:** Keep OverHero as ratified (rejected by Caelan); fold Secondary into Auth so `In` always carries it (rejected — quiz breaks it); a boolean `Show Secondary` property (viable, but a whole tier is structural and Caelan asked for a variant; the code-composition caveat is identical either way); swap boards to `In+Off` + keep the separate SecondaryNav (rejected — wouldn't use the variant, and In+On tested clean).
- **Affected:** DS library masters (TopNav/PillButton/form family); Dashboard file `7t46ROAv93lIQRspgaslgz` (3 board navs swapped); reverses D-042 ruling 3 and narrows its SecondaryNav stance. Pending: Caelan's Publish №2, retirement of old masters `262:30`/`184:8`/`183:2`, FIGMA_MAPs ×3. FIGMA_MAP must note `Secondary=On` maps to composing `<SecondaryNav/>` in code, not a `<TopNav>` prop. Chip swap was moot — zero MetaChip/GoldChip instances in the dashboard file.

### D-043 — CtaButton corner radius settles at 8px
- **Decision:** The dashboard-6px vs quiz-8px radius question left open in D-042 settles at **8px** (`radius/md`), token-bound on the built `CtaButton` masters.
- **Why:** Caelan's call at the Pass-1 plan review; 8px reads as the shared rounded-rect CTA geometry across surfaces.
- **Alternatives:** 6px (the dashboard's prior value) — rejected.
- **Affected:** `CtaButton` set (`608:7`, built previous chat, radius verified bound this session). One-line code ripple deferred to Pass 4: `CtaButton.tsx` `rounded-sm` → `rounded-md` (17 call sites already extracted).

## 2026-07-03

### D-042 — Componentization Pass 0: the promotion registry is ratified; the four open questions settled
- **Decision:** Pass 0 (audit + promotion registry) is done and the registry is **ratified by Caelan** at **`rc-design-system/REGISTRY.md`** (with a pointer in its `conventions.md`). The rulings: (1) **two button sets** — `CtaButton` (rectangular: Style=Filled/Outline × Color=Teal/Gold × Size=lg/md, shipped combos only; the dashboard-6px vs quiz-8px radius call settles at the Pass 1 master build) and `PillButton` (Surface=Light/Dark × Fill=Gold/Teal/Outline); the kit-verbatim Figma `Button` becomes a kit-provenance reference, not-for-app-use. (2) **Chip fold** — one shared `Chip` carrying only the 5 shipped combos + a `Show Icon` boolean, absorbing and retiring `MetaChip` + `GoldChip`; `CountBadge`/`NewBadge`/`StatusPill` stay distinct. (3) **Nav OverHero kept** — the mega set carries `Scroll=Solid/OverHero`; OverHero = the Auth=out design with the bar background removed, lightly adapted for legibility (text/ink switches); only shipped combo is Out+OverHero. (4) **Registry home = `REGISTRY.md`**, a new root file.
- **Why:** Call-site evidence drove each call. Buttons: the repos split into exactly two shipped geometries (dashboard rectangular 6px everywhere; quiz results chrome and every robotics_career CTA are 36px pills), and a single set with a Shape axis would multiply speculative combos against the variant-discipline ruling. Chips: the code atom allows 32 combos but ships 5, and code already retired GoldChip into `Chip tone="gold"` — Figma follows the code taxonomy. OverHero: the audit recommended dropping it (1 of 11 screens), but Caelan overruled — the marketing hero treatment is a real designed state, and the spec is cheap (the Auth=out master minus its background plus ink switches). Registry as its own file because `conventions.md` is token-tier prose and the registry is the citation target for Passes 1–4.
- **Alternatives:** One button set with a Shape axis (rejected — speculative matrix); folding `NewBadge` into `Chip` too (rejected — ripples the published `SecondaryNav/Pill` set for no payoff); dropping OverHero (the audit's recommendation, overruled); a `conventions.md` section for the registry (rejected — buries it).
- **Affected:** `rc-design-system/REGISTRY.md` (new) + `conventions.md` (pointer line); `COMPONENTIZATION_RUN.md` (ledger tick, nav spec finalized, outcomes section); `STATUS.md`; session note 2026-07-03. Audit facts recorded for later passes: Icon union = 61 names (47 + 10 explore_floor + 4 robotics_career live extras); explore_floor's `ProgressBar`/`SegmentedControl` orphaned at zero call sites (deletion candidates); the nav reference image's Sign Up CTA is a rounded-rect gold button (a `CtaButton`, not a pill). Read-only outside the registry and this bookkeeping — no `src/`, no Figma changes.

### D-041 — Figma componentization run ratified: extend the dashboard's component model across the ecosystem files
- **Decision:** A cross-file **Figma componentization pass** is GO, planned in **`COMPONENTIZATION_RUN.md`** (6 single-session passes; Pass 0 = audit + promotion registry, ratified by Caelan before build). The rulings, all Caelan 2026-07-03: (1) real Figma components for anything reused within/across the Interest Quiz and Captures files, extending the dashboard's proven D-044 model (naming contract, variant axes, FIGMA_MAP registration); (2) **one nav mega set** — explore_floor's centered-search `AppHeader` design wins the top tier everywhere, `Auth=in` (profile pill) / `Auth=out` (Resources + Sign In + gold Sign Up; reference `docs/reference/nav-logged-out-target.png`), dashboard's `SecondaryNav` stays dashboard-only, Kayla's `TopNavV2` retired, non-nav headers not forced in; (3) **buttons standardized across the board** (code ripple accepted; robotics_career's stay local-for-now; lean two sets — rectangular CTA + pill — pending Pass 0); (4) **dashboard chip sprawl consolidated**, not mirrored; (5) robotics_career's **form family promoted**; (6) **Icon name union in `@rc/ui`** (the one code-atom exception); (7) **quiz vocabulary stays local** to the Interest Quiz file; (8) **no Figma-mode restructure for dark** — `Surface` variant axis only where a component genuinely ships both.
- **Why:** The Interest Quiz eyeball pass (its one note) surfaced that recurring elements are loose frame contents; named component instances are the design-system story the Fivestar handoff files should tell. Cross-file decisions (what's shared vs local) can't be made per-file, and the dashboard's library already holds 22 published sets — the model is proven, so the pass extends rather than invents. Figma-side only: component instances are client-visible in the handoff, `@rc/ui` code atoms are not.
- **Alternatives:** Componentize each file in isolation (rejected — the promote-vs-local call is inherently cross-file); the full `@rc/ui` atoms tier alongside (rejected — unchanged stretch, zero client-visible payoff); Figma light/dark modes (rejected — invalidates 113 documented variable bindings for nothing); merging all navs including non-nav headers into one set (rejected — only literal nav bars join).
- **Affected:** `COMPONENTIZATION_RUN.md` (new), `docs/reference/nav-logged-out-target.png` (new), `ECOSYSTEM_RUN.md` (Stretch pointer), `REMAINING_WORK.md` (router row), `STATUS.md`, session note 2026-07-03. No `src/` or Figma changes yet — planning only.

### D-040 — Pass 7 remainder closed: extension-captured, variable-bound rebuilds replace the stills; steps 7–8 done
- **Decision:** Pass 7 is fully closed. (1) The Interest Quiz file's screens are now **9 editable, variable-bound frames** (the 8 planned states plus the `SceneSortView` scene-intro state Caelan added): captured by **Caelan with the official Figma Chrome extension** against the production preview at 1440×900, then integrated by an MCP Plugin-API pass — frames renamed to the §2 naming contract (`Landing`, `MCQuestion`, `SceneSortView`, `BucketSort`, `ResultsPanel`, `CompareView`, `ResultsMap`, `ResultsConstellation`, `JobOverview`) and **502 paints bound to library variables** (the FIGMA_MAP §4 dark set + `ARM Gold`/`Near Black` light primitives for the header chrome; on-accent whites to the `Role/* On` aliases). The `Ref/*` stills were **deleted** after per-frame visual verification — Caelan's replace ruling supersedes the §6 keep-beside contract. (2) Ledger step 7 was recognized as done cross-repo (the UX repo's D-007 capture-target session). (3) Step 8 landed: new Claude Design project **"RC.org Site — UX Refinement"** (`a28efdc8-57d4-4730-aec6-38e5141b19f5`) carrying the `@rc/ui` Mode B closure + `REFINEMENT_BRIEF.md`, with the `.design-sync/` trio (config/conventions/NOTES) in `robotics_career`.
- **Why:** The MCP `generate_figma_design` pipeline stalled at the submit step a second time, and the extension is the same capture engine behind an interface that demonstrably works on this account (it produced clean layer trees: real text in the product fonts, honest glass alphas, 28–269 nodes per frame). Capture provenance doesn't matter to the exit gate — "captures bind to variables, not raw values" is met by the bind pass, whoever clicked capture. Replace-not-keep because the bound frames verified pixel-faithful against the live app, making the stills redundant weight in a handoff file.
- **Alternatives:** Retry the MCP capture with the war-story mitigations (attempted; Caelan pulled the plug when it stalled and captured all 9 states himself in minutes). `use_figma` hand-builds from source (the D-007 method) — unnecessary once the extension output proved clean, and a multi-session job for 9 dark screens. Keep the stills beside the rebuilds — overruled by Caelan (replace).
- **Deliberate keeps inside the captures:** `#595959` (`--color-text-subtle` — a code token never published to the library; same class as `--color-white`), one `white/10` map fill, gradients (gold avatar, ambient orbs, bubble glows), vector geometry. The score spread is Technician 82 / Specialist 80 / Integrator 80 (the stills' Specialist-45 spread wasn't reproduced; believable is the bar, per the capture command).
- **Affected:** Interest Quiz file (9 frames, stills gone), `docs/figma/FIGMA_MAP.md` (preamble + §6 rewrite), `ECOSYSTEM_RUN.md` (Pass 7 row + exit gates all ✅, steps 7–8 annotated), `ROADMAP.md` §4.6 (done), `STATUS.md`, `LESSONS.md` L-009, `robotics_career/.design-sync/*` (new, uncommitted for Caelan). No `src/` changes; gates unaffected.

## 2026-07-02

### D-039 — Pass 7 executed: dark set published, Interest Quiz file + FIGMA_MAP live, precedence flipped; captures are reference stills
- **Decision:** Ecosystem Pass 7's core is done and the D-038 flip is live. (1) The dark extension is **published in the DS library** (`afi5Q5nFtcnT9HJ04Cbylg`): 24 color variables — 17 raw in `Primitives`, 7 `var()`-references as `Semantic` aliases, mirroring how code models them — plus the 2 dark effect styles, every variable with an `extension:` lineage description, narrow scopes, and `var()`-wrapped WEB code syntax; verified value-exact against `globals.css` before Caelan's publish click. The library's Foundations/Color page gained a "Dark extension" section (24 variable-bound swatches on a Dark Canvas backing) and the Elevation page a dark shadow row. (2) The **Interest Quiz file** (`pjgrRJS5YYII1iciW7Pak2`) exists in the ARM team, subscribed to the library, with Cover / Quiz Flow / Results pages and **reference stills of all 8 final screens** (1440×900 image fills from the production build, reduced-motion settled states). (3) `docs/figma/FIGMA_MAP.md` is seeded as the binding manifest; `/capture-figma` + `/pull-figma` were rewritten to read it first. (4) `DESIGN_SYSTEM.md` §2/§15 flipped to the three-artifact precedence (library wins values / doc wins usage / code wins behavior), gained §16 keeps + §17 upstream proposals, and the two harness precedence lines now defer to §2 (the D-038 authorization). **The editable, variable-bound screen rebuilds are the pass's recorded remainder** — the stills are visual ground truth, not editable frames; don't cite them as more.
- **Why:** Publication is what makes the flip true (D-038's whole timing argument). The stills exist instead of editable captures because the `generate_figma_design` HTML-capture pipeline proved unusable in this environment: its single-use capture IDs expire in minutes, an interrupted submit burns the ID into a permanent "pending", and one run reported a completed node that never materialized — compounded by macOS occlusion throttling stalling every capture ~150s until the window was fronted. Screenshots + image-fill frames are deterministic and landed in minutes once diagnosed.
- **Alternatives:** Keep fighting the capture pipeline with mint-just-before-use IDs — rejected after it produced zero nodes across a whole evening; the failure modes compound. Rebuild all 8 screens as editable frames in-session — rejected for scope at the hour; the manifest records exactly where rebuilds land beside their `Ref/*` twins. Skip stills until rebuilds — rejected: the handoff file would hold nothing visual meanwhile.
- **Affected:** DS library (variables/styles/Foundations pages), the new Interest Quiz file, `docs/figma/FIGMA_MAP.md` (new), `DESIGN_SYSTEM.md` (preamble/§2/§12/§15/§16/§17), `.claude/commands/capture-figma.md` + `pull-figma.md` + `.claude/agents/doc-steward.md` + `.claude/commands/revise-doc.md`; steward-reconciled: `ARCHITECTURE.md` §3/§7, `ROADMAP.md` §4.6, `HANDOFF_GUIDE.md` §6, `DESIGN_SYSTEM_RUN.md` §1/§2, `HARNESS.md` §3, root `CLAUDE.md`. Ledger/STATUS ticked separately. No `src/` changes; gates unaffected.

### D-038 — Pass 7 adopts the dashboard's DS conventions wholesale: FIGMA_MAP manifest, post-publication precedence flip, keeps/proposals sections
- **Decision:** Pass 7 widens from "publish + capture" to the full convention mirror of `career_dashboard`'s design-system setup, ratified by Caelan after a side-by-side review. Four parts: (1) seed `docs/figma/FIGMA_MAP.md` per the dashboard's naming contract — PascalCase React name = Figma component-set name, `@theme` custom property = the variable's web code syntax verbatim, and **every published variable's description carries lineage** (`kit: <name> (verbatim)` or `extension: <reason, with evidence>`); `DESIGN_SYSTEM.md` §2's dark table is the publication worklist and the manifest seed. (2) **After** publication, flip §2's precedence to the dashboard's three-artifact model (Figma wins values, the doc wins usage, code wins behavior) and add a §12-style precedence section — D-037's code-outward canon is explicitly the *pre-publication* state and is superseded at that moment, not before. (3) Add compact dashboard-§3.6-style "reserved tokens + deliberate keeps" and §3.7-style "upstream proposals" sections (the `@rc/ui` v1.1 candidates: `control-xl`/`control-tap`, the montserrat-500 face, the Icon name union), consolidating what's currently scattered across §5, globals comments, and the ledger. (4) Reword the two flagged harness precedence lines (`.claude/commands/revise-doc.md`, `.claude/agents/doc-steward.md`) to **defer to §2** rather than hardcode a winner — Caelan authorizes this reword here for the Pass-7 session; if the self-modification guard still blocks it, he applies the two lines by hand.
- **Why:** The dashboard's model is the proven end state (its FIGMA_MAP round-trip is wired, published, and audited); explore_floor's code-outward D-037 is the same convention at an earlier lifecycle stage, not a competing one. Folding the mirror into Pass 7 lands each piece exactly when it becomes true — flipping precedence before a Figma mirror exists would re-create the dead-file fiction D-037 just retired. It also settles the harness-line flag without an interim edit: the lines become substantively correct again the moment the flip happens.
- **Alternatives:** Mirror the doc sections now, pre-publication — rejected: a §12 block would declare a Figma authority that doesn't exist for the dark system yet, and the docs would be edited twice. Keep code-outward permanently post-publication — rejected: it diverges the two repos' conventions and demotes the captures to a dead-end copy instead of a maintained mirror. Edit the harness lines immediately — rejected as unnecessary churn: they're wrong for exactly one pass and correct on both sides of it.
- **Affected:** `ECOSYSTEM_RUN.md` Pass-7 section (respecced), `STATUS.md` (Next-up + the awaiting-Caelan list), `DESIGN_SYSTEM.md` §2 (one-line forward pointer), the Pass-6 session note (flag superseded marker). No code; gates unaffected.

### D-037 — Retire RC-CC as the token source: the `@theme` block is canonical, flow is code-outward (Pass 6 doc reconcile)
- **Decision:** `DESIGN_SYSTEM.md` §2 no longer names the RC-CC Figma file as "the source for tokens"; **the `@theme` block in `src/styles/globals.css` is canonical for this repo's token values, and the sync flow is code-outward.** The kit light values live in the Design System library (`afi5Q5nFtcnT9HJ04Cbylg`); the dark extension publishes there as **additional named variables, not a second mode** (matching D-029's additive model) in ecosystem Pass 7; captures land in a new Interest Quiz file (never RC-CC, never Kayla's file). In the same reconcile, §7 was rewritten to the live kit soft shadow tiers (DEF-013 R1 — the Material triple-stacks the section still documented were replaced in code during the realignment) with the dark shadows added, §2's mapping table gained the entire dark system (~30 tokens: surfaces, on-dark ramp, glass, constellation line, the 12 role derivatives, plus effects noted as styles/code-only) which it had omitted, and §1's "package is deferred" line was corrected (`@rc/ui` is live per D-035/Pass 2; consumption here stays stretch-only).
- **Why:** This is ecosystem Pass 6, the pre-sync pass: Pass 7 publishes dark variables and captures screens, and the doc gap was the actual sync blocker — a capture run guided by §2 would have looked for a dead file (RC-CC verified via MCP as one blank cover, zero variables) and missed the whole dark system. "Figma wins for tokens" described the Phase-0 world; since the dark re-architecture, every token value has been authored in code first.
- **Alternatives:** Resuscitate RC-CC as the capture target — rejected in `ECOSYSTEM_PLAN.md` §5 (nothing worth saving; it subscribes to a different, ripped library). Publish dark as a Figma mode on the existing variables — rejected: code models dark as additive named tokens (D-029), and the dashboard file subscribing to the library shouldn't inherit a dark mode it never renders.
- **Affected:** `DESIGN_SYSTEM.md` §1/§2/§5/§7 (owner); steward-reconciled: `ARCHITECTURE.md` §3/§7 (code-outward wording, `typeScale.ts` in the /lib tree), `ROADMAP.md` §4.6 (capture destination note), `docs/knowledge/HANDOFF_GUIDE.md` §6 + `DESIGN_SYSTEM_RUN.md` §1 (package tense + superseded source-of-truth block). Flagged, not edited (harness self-modification — Caelan's call): the stale "Figma wins for token values" precedence line in `.claude/commands/revise-doc.md` and `.claude/agents/doc-steward.md`. Archive untouched.

### D-036 — Control-height token ladder: dashboard's sm/md/lg adopted verbatim, xl/tap minted; remaining raw steps ratified
- **Decision:** The Tailwind-default sizing steps serving as interactive-element heights are now tokens: `--spacing-control-sm/md/lg` (24/32/36) adopted **verbatim from the dashboard's ladder** (`career_dashboard` globals / `@rc/ui`), plus two steps minted here on the same ladder — `--spacing-control-xl` (40, the nav search field) and `--spacing-control-tap` (44, minimum comfortable tap target: the results hero prev/next arrows). Swept: the six results pill buttons `h-9`→`h-control-lg`, the search field `h-10`→`h-control-xl`, the arrows `h-11 w-11`→`size-control-tap`. The remaining numeric steps are **ratified as raw non-control sizing** (nav logo image `h-10`, profile-pill avatar `h-7 w-7`, meter/dot/tile geometry) — layout and decoration, not controls. `xl`/`tap` are noted as `@rc/ui` v1.1 candidates so a future consumer conversion stays mechanical. The compliance rubric's space-scale check now names the ladder and the ratification.
- **Why:** Caelan's ruling on the Pass-6 preference call: snap to the existing dashboard token set where anything is close, mint on the same ladder where nothing is (rather than the run-sheet's ratify-everything default). 36px mapped exactly to `control-lg`; 40/44 had no close token (`size-sq-lg` 40 is a square-tile size, the wrong semantic), so they extend the ladder instead of joining it loosely. Doing this before Pass 7 means captures bind control heights to variables, and keeps the vocabulary identical across the three repos.
- **Alternatives:** Ratify all ~10 steps as raw (the run-sheet default) — superseded by Caelan's ruling. Reuse `--size-sq-lg` (40) for the search bar — rejected: tile-size semantic, and the dashboard's control comment explicitly separates control heights from coincident values. Mint a `control-2xl` instead of `tap` — `tap` names the 44px accessibility floor, which is the actual reason the arrows are 44.
- **Affected:** `src/styles/globals.css` (@theme, five tokens + comment), `AppHeader.tsx`, `RoleHero.tsx`, `CompareView.tsx`, `ResultsExperience.tsx`, `CompareTargetMenu.tsx`, `JobOverview.tsx`; `DESIGN_SYSTEM.md` §5 (new "Control heights" subsection) + §2 mapping row; `docs/rubrics/design-system-compliance.md` (space-scale check + §4). Measured post-sweep: pill 36px, search 40px, arrows 44×44 — pixel-identical, zero console errors; all gates green.

### D-035 — Ecosystem run GO: tokens-first package, multi-pass execution, asset rescue done
- **Decision:** The `ECOSYSTEM_PLAN.md` strawman (2026-07-01) is **ratified**: go on the `rc-design-system` package, scoped **tokens-first** (kit `@theme` + the D-029 dark extension + fonts + base + `Icon` as the sole atom; the full atom tier later; step-5 consumer conversion is stretch-only), then excavate Kayla's two branches into `robotics_career` on `@rc/ui`, hand-port the harness (no template repo before handoff), and run the Figma work (dark variables into the DS library as additive named variables, a new Interest Quiz file, no token-sprawl campaign in this repo). Execution is split into **single-session passes** tracked in the new **`ECOSYSTEM_RUN.md`** ledger (Caelan's call: small sessions over one big pass, with a run sheet so each session starts cold). Pass 1 executed today: the 10 expiring Figma asset URLs on `career_dashboard` `origin/homepage-and-explore` were rescued into `robotics_career/public/figma-assets/` (3 PNG + 7 SVG + `MANIFEST.md`) ahead of their ~07-07 expiry. Two standing rulings recorded: the 07-01 Finder deletions were **intentional with nothing to recover**, and **the ARM UI-kit handoff is the Figma files alone** — the Angular `rc-ui-kit` only existed to seed the Figma style guide, which revises `archive/REALIGNMENT.md` §10's Fivestar-guardrail artifact list (was Figma file + Angular kit + prototype).
- **Why:** The package's trigger changed: a third repo is about to exist (Kayla's branches cover the three "unborn prototypes" name for name), so without the package the new repo becomes a fifth token source — the exact sprawl §10 warned about; born-on-system was always §10's largest payoff. The asset rescue couldn't wait for the rest (hard ~07-07 expiry; URLs verified live before download). The multi-pass structure keeps each session's context lean and makes the expensive Pass-1 research (the both-repo token survey, the 27-token dark append set with its two `var()` rewrites, the tsup/committed-dist package design) durable in the ledger instead of re-derived.
- **Alternatives:** One big session doing rescue + package + bookkeeping — rejected by Caelan (context economy; restructured to passes). Excavate first, retrofit tokens after — rejected: retrofitting a repo that already copied tokens is the churn the one-day package avoids. Merge Kayla's branches into career_dashboard main — rejected: couples the marketing site to the dashboard and ripples `competencies.ts` renames into its widgets and tests. Rescue the assets onto Kayla's branch — rejected for `robotics_career/public/` (her branch stays untouched; the files land where the excavation needs them).
- **Affected:** new `docs/knowledge/ECOSYSTEM_RUN.md` (the pass ledger + the full Pass-2 package spec); `ECOSYSTEM_PLAN.md` (header: ratified, flag §7.1 resolved); `DESIGN_SYSTEM_RUN.md` (go/no-go answered GO); `REMAINING_WORK.md` (router row); `STATUS.md`; outside this repo: `Prototypes/robotics_career/public/figma-assets/` (10 files + manifest; not yet a git repo). No code changes; gates unaffected (docs-only in-repo). Parent `Capstone/CLAUDE.md`'s stale RC_Proto folder-map line is flagged for a Cowork session, not edited here.

## 2026-07-01

### D-034 — Adopt ARM Fivestar's four ideas + a domain-mirror path to the AI roles (documented, build deferred)
- **Decision:** ARM's deck "Interest Quiz Update, Release 4.3" details their Fivestar quiz: six pathways across two domains (Robotics + AI) on a 4-dimension model (Where/How/What/Why), a 2×3 grid of one domain axis × three tiers. We (a) adopt four ARM ideas into our design (their result **insight narratives**, their per-pathway **competency clusters**, the **Where/How/What/Why lens** as a coverage audit, and their **everyday-analogy** question style), and (b) sanction a **domain-mirror architecture** as the fast path to the three AI roles: keep the three-tier `categoryScoring` engine and the seven scenes untouched, add a **domain axis** (`robotics`/`ai`) sourced from **two new "Where" everyday-scenario questions**, and derive six `PathwayId = tier × domain` roles at results time (still a weighted multi-role result, no single winner). **This pass is documentation only** (`ARM_FIVESTAR_COMPARISON.md`, `AI_ROLES_INTEGRATION.md`); no code changes, and the `CLAUDE.md`/`DATA_MODEL` hard rules ("three robotics roles", "do not add or rename roles") are **unchanged until a build is green-lit**.
- **Why:** The deck confirms our three tiers are ARM's three robotics tiers and that our robotics competency chips already match ARM's clusters, so the ideas are low-risk and authoritative. Crucially, because each tier shares the same How/What/Why signature across both domains, our engine already scores the "tier" half of all six roles, so the AI roles need only one new signal (the physical-vs-digital domain lean). A domain axis keeps the scenes at three choices (preserving the deepest data invariant) instead of forcing six-choice scenes. This also supplies the answer `HANDOFF_GUIDE.md` §2 said was "not on file": the AI variants are the digital mirror of the three robotics tiers. Building is deferred because it lands at the July 21 handoff and widens scope past robotics-only, a product call to make deliberately, not under handoff pressure.
- **Alternatives:** Six flat roles (`CategoryId` grown to six) — rejected: forces six choices per scene and a full instrument re-author, and breaks the one-per-role scene invariant. Adopt ARM's single-winner + Q4 "Why" tiebreaker — rejected: our weighted multi-role result is a research-backed hard rule (the "maybe group" shouldn't be boxed). Build it now — rejected this pass (handoff timing + scope); Caelan chose memo + architecture spec. Infer the domain from existing interest questions instead of new ones — considered; Caelan chose two dedicated "Where" questions for a cleaner, more reliable read.
- **Affected:** new `docs/knowledge/ARM_FIVESTAR_COMPARISON.md` (comparison + the four adopted ideas with source copy) and `docs/knowledge/AI_ROLES_INTEGRATION.md` (the domain-mirror spec + staged deferred build plan); deck text mirrored at `docs/reference/ARM_Fivestar_Interest_Quiz_4.3.md`; pointers added in `STATUS.md`, the `CLAUDE.md` doc map, and `HANDOFF_GUIDE.md` §2. No source files touched; gates unaffected (docs-only).

### D-033 — Warn-only knowledge-guard hook + snapshot/index conventions for the knowledge layer
- **Decision:** Slim the session-start context and keep it slim. `STATUS.md`'s header is now a tight **snapshot** (`Current focus` / `Next up` a few sentences each; per-session detail lives in the `sessions/` notes, linked from a new `### Earlier sessions` list, never inlined), `DECISIONS.md` carries a 1:1 `## Index` at the top, and a single **warn-only** `PostToolUse` hook (`.claude/hooks/knowledge-guard.sh`, matcher `Edit|Write|MultiEdit`) nudges — never blocks, never edits — when the STATUS header re-bloats (>9KB, or any run-on header bullet >1500 chars) or the DECISIONS index falls out of sync with its `### D-` headings. The session-start ritual is now header-first: read `STATUS.md`, then the newest note's **Resume here** header, the full note only if continuing that thread. Also dropped the inert `firecrawl` entry from `enabledMcpjsonServers` (its plugin is disabled globally).
- **Why:** Every session paid a fixed first-turn token cost before real work started; `STATUS.md` had grown to ~27KB (~79% of it a run-on header) and `DECISIONS.md` (~75KB) had no index, so a lookup scanned the whole file. Relocating detail into the `sessions/` notes and a decisions index (nothing deleted) cut STATUS to ~11KB and made lookups cheap; the guard stops the hygiene from silently regressing. Mirrors the sibling `career_dashboard` job (its commit `cf105a1`), tuned to this repo's run-on-header shape rather than a stacked changelog.
- **Alternatives:** A *blocking* hook (rejected — a guard must never break the flow); *deleting* old history (rejected — it's ARM-handoff + portfolio material, so relocate, don't delete); restructuring the heavy on-demand docs (`REALIGNMENT` / `VISUAL_REARCHITECTURE` / `DATA_MODEL`) in the same pass (rejected — nothing pulls them at session start, so on-demand is already fine).
- **Affected:** `docs/knowledge/STATUS.md` (header rewritten; the live checklist section left byte-identical), `docs/knowledge/DECISIONS.md` (+`## Index`), `.claude/hooks/knowledge-guard.sh` (new), `.claude/settings.json` (hook wired; `firecrawl` dropped), `.claude/commands/compound.md` · `phase-check.md` · `revise-doc.md`, `CLAUDE.md`, `docs/knowledge/HARNESS.md`, `docs/knowledge/README.md`, root `README.md`.

### D-032 — Reorganize REMAINING_WORK into owner/timeframe docs (router + three homes)
- **Decision:** Split the flat `REMAINING_WORK.md` catalog into three purpose-built docs and slim the original to a router. **`HANDOFF_GUIDE.md`** = what ARM/Fivestar must do after handoff (per-job/bridge content swap, AI-role-variant integration, three-role name confirmation, CTA destination) plus the cleanup we owe first (delete `devSeedResults()`). **`DEFERRED_DIRECTIONS.md`** = consciously deferred polish + open decisions to revisit after the v3 test (rung cue, results polish, the scene first-card motion nuance, responsive, a11y, `MAYBE_WEIGHT`/Q0 calls, the drag-removal end-state). **`DESIGN_SYSTEM_RUN.md`** = the `rc-design-system` package build sheet + `/capture-figma` queue for the dedicated design-system run. `REMAINING_WORK.md` becomes a one-screen router pointing at all three.
- **Why:** The single flat list mixed four different owners/timeframes (blocks-us, ARM's, deferred-to-v4, near-term design-system), so nothing was clearly owned and our active plate was unreadable. Splitting by owner + timeframe takes the not-ours items off our queue and gives each a home a reader can act on; the handoff doc also seeds the July 21 client deliverable so it's a transcription later, not a re-derivation.
- **Alternatives:** keep one flat list (rejected — the mixing is the problem); a `docs/knowledge/forward/` subfolder (rejected — three top-level UPPER_SNAKE docs match the existing knowledge-doc convention and are individually discoverable); delete `REMAINING_WORK.md` (rejected — STATUS + memory link to it, so it stays as a router).
- **Affected:** new `docs/knowledge/{HANDOFF_GUIDE,DEFERRED_DIRECTIONS,DESIGN_SYSTEM_RUN}.md`; `REMAINING_WORK.md` rewritten to a router; `STATUS.md` Phase-3 pointer + the `project-realignment-direction` memory updated to point at the new homes.

### D-031 — Remove the dormant drag path (DragSortCard / DropZone) as dead code
- **Decision:** Delete `src/components/DragSortCard.tsx` and `DropZone.tsx` and their two barrel exports; the narrative rater stays click-only (`BucketSort`, tap/Enter). Reverses `VISUAL_REARCHITECTURE.md` §3 rule 4 / §10's "keep the drag path compiling and tested-enough to revive" dormancy guarantee.
- **Why:** The components had **zero importers and zero tests** — nothing rendered them, and `BucketSort` was rewritten click-only in Phase B — so the "tested-enough to revive" guarantee was never actually met. Keeping them was a false comfort (dead code that reads as a live seam), not a real one; reviving a drag interaction would be a fresh build regardless. ARM rebuilds from the prototype anyway, and git history + the `archive/pre-narrative-only` tag preserve the old code. Removing it is honest and shrinks the surface.
- **Alternatives:** keep dormant + add a smoke test to make "revivable" honest (rejected — the test would guard code no path uses, and reviving still needs rewiring from scratch); keep as-is and document the untested state (rejected — still dead weight handed to ARM). Caelan's call: remove.
- **Affected:** deleted `src/components/DragSortCard.tsx` + `DropZone.tsx`; `src/components/index.ts` (two exports removed); `BucketSort.tsx` comment updated to click-only; end-state recorded in `DEFERRED_DIRECTIONS.md`. Gates green (lint, typecheck; full suite in the same pass).

## 2026-06-30

### D-030 — Quiz-flow legibility pass (scenario-first hierarchy, factual-detail placement, token-only sizes)
- **Decision:** A polish pass on the live narrative quiz, from two rounds of Caelan feedback against the imported Claude Design reference. **(1)** Scene screens **invert the scenario/question hierarchy**: the scenario is the headline (token **h4 24 → body 16** on the intro→rating morph, Montserrat 600, white) and the question drops to a smaller white line below it (token **h5 20 → body 16**, Roboto 400, no gold). **(2)** MC steps gain optional `promptPlacement: 'above' | 'below'`; factual qualifiers (the salary range) render **below** the question as a quiet `text-small` line, scenarios/transitions stay above. **(3)** The college-duration screener is rewritten **self-contained** ("How long would you want to attend college?"); Q5's "Workers in robotics…" lead-in dropped; Q6's lead-in trimmed to "Okay, one last thing." **(4)** The choice rating card drops from `text-h4` (24/700) to **`text-body` (16/600)** — deliberately **below** the reference's 20px — and its 2-line min-height reserve was removed (at 16px every scene label fits on one line; measured live, longest ~442px in a ~558px card; card 122→72px). **(5)** "Question N of M" is computed **index-based** (answered MC steps before the current step), fixing the stuck "6 of 6" on a Back revisit.
- **Why:** V2 research: the scenario/context line above the bold question gets skipped, leaving people unsure what the question asks. Making the scenario lead frames the question; a factual line reads as detail, not scene-setting, so it belongs below; self-contained wording survives even when the line is skipped. Token-only sizes keep the type scale honest (a 17/18px had crept in). Caelan's live read: the 20px choice card dwarfed the scene recap and wrapped to two lines.
- **Alternatives:** keep the reference's order + 20px choice card (rejected — the reference carries the same skip risk, and the card read too big in context); the lighter mockup options (bigger/brighter, eyebrow label, accent bar — A/B/D, not chosen); prompt-above for the salary (rejected — factual, belongs below); keep the question muted grey (Caelan asked for white, matching the prototype).
- **Affected:** `screens/Flow/SceneSortView.tsx`, `MCQuestion.tsx`, `BucketSort.tsx`, `FlowRunner.tsx`; `data/types.ts` (`MCStep.promptPlacement`); `data/flows/narrativeFlow.ts` (q2 wording, q3 below, q4 prompt removed, q5 trimmed); `tests/e2e/narrative.spec.ts`. Canonical CD reference imported to `docs/reference/claude-design/` (the `.dc.html` + `support.js` + README the harness cites). Gates: lint ✓, typecheck ✓, 82 unit ✓, 3 E2E ✓.

### D-029 Phase F — the job constellation + job overview (executed)
- **Decision:** Build step 8's **Phase F** — screens 6 + 7 of the mockup's 5-screen results system: the per-role **job constellation** (`view: 'selected'`), the **job overlay** layered on it (`view: 'job'`), and the standalone **job overview** page (`view: 'job-overview'`). Locked calls (with Caelan, AskUserQuestion): **(1)** **distinct per-job placeholder** content authored in the project voice (each job: own summary + ~3 responsibilities + ~4 skill chips), **not** the mockup's shared placeholder (where every job reads the same), reusing the real ARM role salary/education/competencies + the existing bridge-program placeholder; all per-job copy banner-marked for ARM swap. **(2)** the map bubble now **dives into the constellation** (restoring the mockup's loop), revising Phase E's interim bubble→cards; the constellation's "Role overview" then routes to that role's cards (marking `fromMap`). **(3)** **count-aware** constellation — N nodes ring the center on an even polar orbit, honoring ARM's real common-title counts (Technician 3 / Specialist 5 / Integrator 5), **not** the mockup's fixed 4 corners. Build calls: F2 + F3 **merged** into one slice (the constellation + overlay share one mounted shell, so `selected`+`job` are one keyed `motion.div` — `key="constellation"` — and the constellation never remounts between them; only the side-panel body swaps via its own AnimatePresence); the job overview reuses `ResultsPanel` (sticky bar) with **local** tab state (not `nav.activeTab`); the "Set as target role" pill is **inert** chrome (no real wiring per scope).
- **Why:** Phase F was the last build phase of step 8 and is faithful to the mockup as the visual source of truth. Distinct placeholder makes a far stronger demo + user test than the mockup's shared text (which the README itself flags as placeholder), at the cost of authored copy that's clearly marked for ARM. Bubble→constellation restores the mockup's intended spatial loop now that the constellation exists (Phase E only went to cards because it didn't). Count-aware honors ARM's real titles instead of padding/trimming to a fixed 4. The single mounted shell is the one subtle correctness point — two `mode="wait"` branches would remount/replay the constellation between `selected`↔`job`.
- **Verified:** gates green — **lint, typecheck, 74 unit** (+7 pure `constellationLayout`; `data-integrity` extended: `explore` copy-walk, `overviewTabs` length, a per-job invariant block 3/5/5), **3 E2E** (`narrative` walks bubble → constellation → node → job overlay → job overview → 3 tabs → back chain → "Role overview" → cards → retake; `reduced-motion` dives + opens a job with normal clicks), build, zero console errors; live Playwright screenshots (constellation, job overlay, all 3 overview tabs). Graded `/design-review`: **PASS on both rubrics, zero p1**; one p2 **fixed** (inactive node-star glyph `accent.text` → `accent.textSoft` to clear the 3:1 graphical-contrast bar on the glass fill — teal was 2.64:1); three p3s deferred to Phase G (gold-reserved co-occurrence note, a side-panel %-definition caption, the rung framing surfacing only on the overview tab).
- **Alternatives:** mockup-faithful **shared** placeholder (rejected — every job page reads identically, weak for testing); **keep Phase E's** bubble→cards + add a cards CTA to the constellation (rejected — less faithful to the mockup loop); **fixed 4** corner nodes (rejected — would pad Technician's 3 / trim the 5s, less honest than ARM's counts); two separate `mode="wait"` branches for `selected`/`job` (rejected — remounts the constellation); wiring real ARM job data now (not available — the data-request template is still out, so placeholder + swap-later).
- **Affected:** `src/data/{types,jobs,index,flows/narrativeFlow}.ts` (new `Job` + `ResultsExploreCopy`, `jobs.ts` 3/5/5 placeholder, `explore` copy block); new pure `src/lib/constellationLayout.ts` (+ test, barrel); `src/screens/Results/cards/*` (new `ResultsConstellation`/`ConstellationField`/`ConstellationNode`/`JobSidePanel`/`JobOverview`/`TrajectoryViz`/`EducationList`; `useResultsNav` + `ResultsExperience` extended; `diveToRole` → `openConstellation`; `RoleTabRole` now uses `EducationList`); `src/styles/globals.css` (`container-constellation`/`container-job-panel`); `data-integrity` + `fixtures` + narrative/reduced-motion E2E; `VISUAL_REARCHITECTURE`/`DATA_MODEL`/`DESIGN_SYSTEM`/`STATUS`. Branch `narrative-v3-realign`, no commit (left for Caelan). **All five mockup results screens are now built; next is Phase G (polish/motion/responsive/a11y).** Handoff `sessions/2026-06-30-phase-F-constellation-job-overview.md`.
- **Superseded 2026-06-30 (count only):** locked call (3)'s **count-aware 3/5/5** ring was reversed to a **fixed 4/4/4** — four featured jobs per role — in the same-day results-quiz-polish pass, restoring the mockup's four-corner balance (the "fixed 4" that alternative #4 above had rejected). Technician gained a 4th title (`technician-robotics-maintenance-technician`, flagged for ARM confirmation); Specialist + Integrator each feature 4 of ARM's 5 (one title dropped from each ring). Live everywhere now: `jobs.ts` = 12 jobs (4/4/4), the `data-integrity` per-role invariant asserts `{4,4,4}`, and `docs/reference/Job_Program_Data_Request.md` requests 4 per role. The rest of this entry stands.

### D-029 Phase E — the ambient bubble map (executed)
- **Decision:** Build step 8's **Phase E** — replace the stubbed `map` view with the mockup's ambient **bubble map** (`screen: 'map'`), screen 5 of the 5-screen results system. Locked calls (with Caelan, AskUserQuestion): **(1)** scope = **Phase E only** — the constellation + job-overview (Phase F) stay parked because they need ARM's per-job/program content; the bubble map is the only remaining results screen not blocked on that data. **(2)** layout = **rank-based** — the top match is the largest bubble, high and centred, the 2nd/3rd flank below, size scaled by match % (mirrors the mockup's `BUBBLE_POS`), **not** the role-fixed `/select` triangle (`CATEGORY_ANGLES`). **(3)** **Delete the dead node-map** (`CategoryResults` / `NodeMap` / `FitNote`) now that the bubble map supersedes it; keep `FitRadar` + `RoleDetailSheet` + `lib/nodeLayout.ts` (live in `/select`). Plus: a bubble click **dives into that role's cards** (no constellation yet) via a new `useResultsNav.diveToRole` + a `fromMap` flag that flips the cards control bar to a **"Back to the map"** pill (the mockup's `fromMap` chrome); the map is **full-bleed** so the results `<main>` relaxes its rounded `max-w-lg` panel for the `map` view; motion (bubble float + ambient orb pulse) is **reduced-motion-gated** and uses `easings.soft`.
- **Why:** Phase E is the unblocked next step in the plan of record (`VISUAL_REARCHITECTURE.md` §6) — F is data-dependent. Rank-based foregrounds the user's own result (the mockup's intent) and keeps the map distinct from the role-fixed `/select` radar. Diving to cards (vs a constellation stub) avoids building on placeholder job data that would be reworked in F. Deleting the node-map resolves the long-parked "decide its fate" call and clears dead code (all three files were unreferenced except a stale comment).
- **Verified:** gates green — **lint, typecheck, 65 unit** (+4 pure `bubbleLayout`; `data-integrity` copy-walk extended for the nested `map` block), **3 E2E** (`narrative`/`reduced-motion` open the map → 3 bubbles → dive into the top match → `role-name` updates + `back-to-map`), build, zero console errors; live Playwright screenshot (rank-based layout, circular bubbles, role glows, AA labels). Graded `/design-review`: **PASS on both rubrics, zero p1/p2** — only token-hygiene polish, fixed this session (idle-loop easings → `easings.soft`; off-scale fluid bubble text + decorative glow/orb literals marked intentional), p3s (an `ambient` duration token, the "moments" echo, full responsive) deferred to Phase G. Reduced-motion verified live (float + pulse pin static).
- **Alternatives:** push Phase E **+ F** with placeholder job data (rejected — risks rework when ARM data lands, much larger pass); **role-fixed triangle** layout (rejected — rank-based foregrounds the result and stays distinct from `/select`); bubble click opens a **constellation stub** (rejected — that's Phase F territory, no data yet); **keep the node-map parked** on disk (rejected — dead code, the bubble map replaces it).
- **Affected:** `src/screens/Results/cards/*` (new `ResultsMap`/`AmbientField`/`BubbleField`; `useResultsNav` + `ResultsExperience` extended); new pure `src/lib/bubbleLayout.ts` (+ `__tests__/bubbleLayout.test.ts`, barrel); `src/data/{types,flows/narrativeFlow}.ts` (`ResultsCardsCopy.backToMap` + `map` block, new `ResultsMapCopy`); `src/styles/globals.css` (`container-map`/`container-map-card`); **deleted** `Results/category/{CategoryResults,NodeMap,FitNote}.tsx`; `data-integrity` + `fixtures` + narrative/reduced-motion E2E; `VISUAL_REARCHITECTURE`/`DATA_MODEL`/`DESIGN_SYSTEM`/`results-screen` rubric/`STATUS`, with `doc-steward` sweeping `PRD`/`ARCHITECTURE`/`ROADMAP`/`CONTEXT_BRIEF`/`CLAUDE`/`README`. Branch `narrative-v3-realign`, no commit (left for Caelan). **Next: Phase F (constellation + job overview, data-dependent), then Phase G.** Handoff `sessions/2026-06-30-phase-E-bubble-map.md`.

## 2026-06-29

### D-029 Phase C — the dark results role-cards screen (executed)
- **Decision:** Build step 8's **Phase C** — re-architect `/results` from the light node map into the mockup's dark **role-cards** screen, the headline of the 5-screen results system. Locked calls (with Caelan): **(1)** scope = **cards screen only**, the Compare/Map control-bar buttons stubbed to a "coming next" placeholder (Phases D/E build them) on a real `useResultsNav` view-state seam. **(2)** **Signal bars, not the radar** — the hero shows ranked horizontal bars from `matchPercentages` (active role in its accent, others neutral), replacing the degenerate triangle radar on the headline (the radar lives on in the `/select` sheet). **(3)** Role name + match % are **neutral on-dark**, the accent lives in the active signal bar (faithful to the mockup; supersedes the old plan text's "role name in accentSoft"). **(4)** **Tab 2 = real per-role ARM competencies** (Caelan: "competencies, not skills" — the term we'd been misusing), pulled verbatim from the "Levels of Competencies" page; bridge programs stay **placeholder** (`bridgePrograms.ts`) pending ARM. **(5)** **Duties authored as `{heading,text}`** from the ARM job activities. **(6)** **Match-% definition** follows the intended screenshot: collapsed = "where your X% comes from" (count out of the 11 moments), expanded = the 01 what-you-chose / 02 openers+moments / 03 tally-not-a-verdict / what-you-passed-on breakdown, wired to the now-live `categoryBreakdown` + `screenerFit`. **(7)** **Technician-as-rung** — the entry result carries a `pathUp` callout (entry point not the ceiling; grow into Specialist/Integrator). **(8)** Standing **voice rule** for all copy (natural/human, no em dashes, no "not X but Y") → memory + repo `CLAUDE.md`.
- **Why:** The three step-8 content goals (define the match-%, frame Technician as a rung, wire `categoryBreakdown`) plus the realignment's "somewhere to go" gap all land here, on the mockup's high-fidelity layout. Real ARM competencies beat placeholder skills; signal bars are the mockup's own answer to the unreadable triangle radar; neutral name/% + accent-in-bar is what the mockup actually does.
- **Verified:** gates green — **lint, typecheck, 55 unit, 3 E2E**, build, zero console errors; live Playwright walkthrough of the full flow (top match, the breakdown reconciling "openers + moments = pointed of 11", both tabs, prev/next with per-role accent switch, the path-up callout, the compare/map stub, a 0-moment role rendering gracefully). Graded `/design-review`: `design-system-compliance` **PASS**; `results-screen` 1 p1 + 1 p2 **fixed** (technician path-up; the 0-moment breakdown no longer asserts "you kept…"), one p3 (signal-bar graphical contrast, mitigated by the printed value) **deferred to Phase G**.
- **Alternatives:** keep the node-map headline re-skinned (rejected — Caelan wants the mockup carried over); accent-colored role name/% (rejected — the mockup is neutral); placeholder skills for Tab 2 (rejected — real ARM competencies exist); build Compare (Phase D) this session too (rejected — scope, fidelity).
- **Affected:** `src/screens/Results/cards/*` (new — `ResultsExperience`/`ResultsPanel`/`RoleHero`/`SignalBars`/`WhyYouMatched`/`RoleTabs`/`RoleTabRole`/`RoleTabSkills`/`StatBox`/`Chip`/`BridgeProgramRow`/`useResultsNav`/`copy`), `Results.tsx`; `src/data/{types,roleDetails}.ts` + new `bridgePrograms.ts` + `flows/{narrativeFlow,screeners}.ts`; `src/lib/categoryBreakdown.ts` (wired + openers/moments/passed); `src/components/{Icon,index}.ts`; `src/styles/globals.css` (`container-results`); the unit specs (+ `__tests__/fixtures.ts`) + narrative/reduced-motion E2E; `DATA_MODEL`/`PRD`/`DESIGN_SYSTEM` + both rubrics + `Job_Program_Data_Request.md`, with `doc-steward` sweeping `ARCHITECTURE`/`CONTEXT_BRIEF`/`ROADMAP`/`CLAUDE`/`README`; memory + `STATUS`. Branch `narrative-v3-realign`, no commit (left for Caelan). **Next: Phase D (results compare).** Handoff `sessions/2026-06-29-phase-C-results-role-cards.md`.

## 2026-06-26

### D-029 — Step 8 = the dark visual re-architecture (plan adopted; locked design calls)
- **Decision:** Open step 8 (the realignment's parked product work) as a **dark visual re-architecture** of the whole quiz, emulating the Claude Design mockup ("Interest Quiz CD Prototype", projectId `df8d5f31-2435-4a09-9382-6af1d62f9b59`, read via the `DesignSync` MCP). Plan of record: **`docs/knowledge/archive/VISUAL_REARCHITECTURE.md`** (7 short phases 0/A–G). The locked calls: **(1)** re-skin the entire flow **dark** to match ARM's live **My Match** flow, and expand the results from the single node-map into the mockup's **5-screen results system** — role cards (hero + tabs + inline "why you matched") → compare → ambient bubble map → per-role job constellation → job overview. **(2)** **Our question set is ground truth** — keep the real 6 intro questions + 7 scenes; the mockup governs *visual/interaction treatment only* (its "5 generic screeners" and four-path data are placeholder). **(3)** Role palette = **kit-only gold/teal/orange** — Technician=gold `#FFB81C`, Specialist=teal `#117289`, Integrator=orange `#BF5309` (mirrors the mockup's Operate→gold / Program→teal / Plan→orange), which **retires `arm-blue`** (the AA-failing temp holdout) and drops the mockup's green. **(4)** **Tokens: local dark set now, shared package later** — author the dark palette in `explore_floor`'s `globals.css`, quiz renders dark-only (no toggle); the shared `rc-design-system` package (`REALIGNMENT.md` §10) stays deferred until after the prototype ships. **(5)** Dark neutral ramp is **additive but never duplicates an existing token** — reuse `#262626`/`#595959`/`#757575`/white/black already in `globals.css`; add only the two genuinely-new ARM-site greys `#1B1B1B` + `#292929`, plus an off-white text ramp and glass/blur tokens. **(6)** Interaction stays **click-to-select; drag code (`DragSortCard`/`DropZone`/`BucketSort`) left dormant** (may return). **(7)** Motion is **subtle/tasteful** (button life, natural sheet expand/collapse, gentle ambient dots), reduced-motion-aware. **(8)** No ALL-CAPS/uppercase eyebrows or stat labels (per the mockup project's `CLAUDE.md`).
- **Why:** The realignment (D-024–D-028) existed to unblock exactly this. The mockup carries a lot of intentional decisions worth preserving, and it *is* the high-fidelity realization of the step-8 content goals already on file (define the match-% in one plain line, frame the entry Technician as a starting rung, wire `categoryBreakdown.ts`, re-introduce role-keyed programs, resolve the degenerate 3-axis radar — the mockup replaces it with horizontal **signal bars**). Kit-only gold/teal/orange satisfies the realignment's "restrained, teal-led role palette" target while matching the mockup; the additive-no-duplicate token rule is Caelan's explicit call. Local dark tokens keep the timeline (handoff late July) ahead of the shared-package lift.
- **Alternatives:** Core-3 results (cards+compare+map, defer constellation/job-overview) or just re-skinning the current node-map (rejected — Caelan wants the full mockup carried over, will source the per-job + bridge-program data from ARM); add a 4th hue / two-brand-plus-neutral palette (rejected — gold/teal/orange is the clean kit-only set); stand up the shared `rc-design-system` package first (rejected — too large an upfront lift for the timeline); dual-theme the quiz with a toggle (rejected — it's going dark to match My Match, no light variant needed).
- **Affected (planned, not yet built):** `src/styles/globals.css` (dark palette + glass + role accents), `src/components/categoryAccent.ts` (gold/blue/teal → gold/teal/orange + soft/onAccent/glow), `/public/fonts` (real Montserrat/Roboto/Material Icons), a new `AppHeader` shell + the five results views as an internal state machine under `Results/`, enriched `roleDetails.ts` + new per-role **jobs** and **bridge-programs** data (ARM-sourced via the §8 data-request template), and the E2E/`data-integrity` specs each phase. This entry records the **direction**; per-screen confirmations are the 23 open questions in `VISUAL_REARCHITECTURE.md` §9. Nothing implemented yet — this is the Phase 0 plan-landing. Builds on D-024 (kit tokens) and the step-8 goals in D-027/D-028.

## 2026-06-25

### D-028 — Realignment Phase 5: collapse the four categories to ARM's three roles
- **Decision:** Pivot the live model from the four study categories to ARM's published **three-role** structure. Renamed `CategoryId` `operate/repair/program/plan` → **`technician/specialist/integrator`** (`CATEGORIES` order technician > specialist > integrator). Collapsed the two entry categories (Operate + Repair) into a single **entry Robotics Technician**, built from the old Operate/Operator card (description + activities verbatim), HS/GED, **national median $45,936**; **Program → Specialist** ($85,000–$147,700, median $105,000, Bachelor's); **Plan → Integrator** ($87,000–$153,000, median $99,250, Bachelor's) — all education + salary refreshed to `docs/reference/ARM Updated Role Structure - Source Content.md`. Technician `commonJobTitles` = ARM's three (Robot Operator / Entry Level Robotics / Assembly Operator) + two maintenance titles (Automation Technician / Robotics Maintenance Technician). **Content (5a):** re-cut the 7 scenes + Q4/Q5 from **4 → 3 choices**, merging each scene's operate + repair pair into one technician choice (kept the more concrete hands-on line; two genuinely blended — Q4, S1). **Rebuilt the salary question (Q3)** on the real ARM ranges: `$45,000` → technician, `$85,000` and `$105,000+` → specialist + integrator. **Left Q2 "1-2 years" unscored** — it matches no role (above HS, below Bachelor's), which also keeps the three role ceilings equal at **`expectedCategoryMax {11,11,11}`**. **Mechanical (5b):** removed `StatementSortStep`/`SortStatement` and the `CategoryFlow.kind` `'exam'` arm (deferred from Phase 4); derived `zeroWeights()` + `FitRadar` `gridWeights` from `CATEGORIES`; flipped the node-map + fit-radar geometry from a 4-axis diamond to a **3-axis triangle** (`CATEGORY_ANGLES` technician −90 / specialist 30 / integrator 150); collapsed `categoryAccent` to 3 keys (technician→gold, specialist→blue, integrator→teal). Rewrote the `CLAUDE.md` taxonomy hard rule to the three roles and **retired the D-017 carve-out** ("Operator" now lives only as a Technician job title).
- **Why:** ARM's live site standardized to three roles, and moving to the narrative flow exclusively (D-027) dissolved the study-integrity objection that had blocked the collapse (no second instrument to keep in sync). Honest role names (the ids now match the role names) for the Fivestar handoff. Equal maxes were a user ask; unscoring "1-2 years" is the equalizer that's also the most faithful mapping.
- **Verified:** all gates green — **lint, typecheck, 49 unit (5 files), 3 E2E** (`narrative` / `role-select` / `reduced-motion`). The name-collision trap was handled (the entry Technician is built from the old Operate/Operator content, never the old `repair` card whose name was already "Technician"); `narrative.spec`'s collision asserts were flipped so the node yielding "Technician" is the Operate-derived entry role at $45,936. Source compiled with zero non-test errors before the test re-baseline. _(An adversarial skeptic pass over the three named risks — name-collision mislabel, triangle geometry, max recompute — is the closing check.)_
- **Alternatives:** keep "1-2 years" scored toward technician for a `{12,11,11}` ceiling (rejected — the user asked to equalize; unscoring is the honest equalizer); keep `StatementSortStep` dormant (rejected — orphaned after the Exam archive, and its switch arms became type errors once removed); split Specialist vs Integrator by salary (rejected — their ranges overlap almost entirely, Integrator's median is actually lower, so salary only separates entry from degreed).
- **Affected:** `src/data/{types,roleDetails}.ts`, `src/data/flows/{narrativeFlow,screeners}.ts`, `src/components/categoryAccent.ts`, `src/lib/{categoryScoring,categoryBreakdown,nodeLayout}.ts`, `src/screens/Results/category/FitRadar.tsx`; the 5 unit specs + 3 E2E specs; `CLAUDE.md` (whole-file sweep, hard rule rewritten), `.claude/agents/verifier.md`, `.claude/skills/data-author/SKILL.md`; the spec docs + the content spec via `doc-steward` (`DATA_MODEL` §17 owner-first, then `PRD`/`ARCHITECTURE`/`DESIGN_SYSTEM`/`ROADMAP`/`README`/`CONTEXT_BRIEF`/`HARNESS`); `STATUS.md` ticked. Follow-up still open: trim the dead `howler` devDependency; **step 8** (the high-fidelity narrative results screen) is unchanged and next.

### D-027 — Realignment Phase 4: strip the build to the narrative flow (archive Classic + Exam)
- **Decision:** Delete the dormant Classic flow and archive the Exam flow from the live tree, leaving the prototype **narrative-only on the four-category model**. Tagged `archive/pre-narrative-only` first as the recovery point. **Deleted:** the classic screens (`Sort`/`RoundBeat`/`Build`/`ClassicResults`/`Pedestal`/`RoleCard`/`ProgramList`/`FourPartRead`), the classic libs (`scoring`/`robotAssembly`/`fit`/`audio`/`programSelection`), the classic data (`items`/`roles`/`robotParts`/`colorSchemes`/`rounds`/`resultsCopy`/`questionSets`/`classicFlow`, plus `programs`/`competencies`/`skills`), the classic components (`MatchIndicator`/`accent`/`RoundIndicator`), `scene/RobotPlaceholder`, `state/useQuestionSet`; the exam flow (`examFlow`, `Results/exam/*`, `StatementSortView`); and the classic + exam E2E (`happy-path`/`compare`/`exam`/`helpers`). **Narrowed:** `FlowId` → `'narrative'`; the registry + `flowList` to narrative-only; `types.ts` pruned of every classic type, `RoleId`, and the classic `SessionState` fields (`currentRound`/`decisions`/`scoreResult`/`robot`/`currentlyTryingOn`/`soundEnabled`); `currentScreen` → `'landing' | 'flow' | 'results'`; the router dropped `/sort` + `/build`; the Landing switcher auto-culls to **Narrative / Select**. **Rewrote** `reduced-motion.spec` onto the narrative flow so reduced-motion coverage survives. **Kept on purpose:** `categoryBreakdown.ts` (pure, tested, unwired until step 8 — the match-explanation engine the results screen wants); `StatementSortStep`/`SortStatement` + `CategoryFlow.kind`'s `'exam'` arm (harmless, cleaned in Phase 5); `src/scene/LandingSceneHint` + the `scene/*` and `arm-blue` tokens (step-8 Landing redesign). **Still four categories** — the 4→3 role collapse is Phase 5. Ran as gates-green groups (tests-first → exam → classic → E2E → docs/gates).
- **Why:** Two 2026-06-25 calls drove it: the team moved the prototype to the narrative flow exclusively, and ARM's live site collapsed our four categories to three roles. The narrative won the June study ("engagement belongs to the narrative, and it wasn't close"), so the Exam comparison arm and the never-shipped Classic conveyor are dead weight; deleting them makes the code match the re-centered specs (D-025) and clears the path for the high-fidelity narrative results screen. Exam couldn't sit dormant the way Classic did, because the Phase-5 `CategoryId` rename would break its typecheck and its 8/7/7/8 invariant. Kept the destructive strip (this phase) separate from the 4→3 content pivot (Phase 5) for a legible diff and bounded context.
- **Verified:** verify-before-delete on every file (confirmed no live importer before each `git rm`); gates green between each group; final full gates **lint, typecheck, 49 unit (5 files), 3 E2E** (`narrative` / `role-select` / `reduced-motion`). `doc-steward` reconciled the seven spec docs (Exam demoted to documented cut, Classic marked deleted-and-recoverable). An adversarial skeptic audited the full diff against the recovery tag: **no runtime breakage, no orphaned code**; it surfaced one stale `ARCHITECTURE §5` line and four stale code comments still naming deleted files (`scoring.ts` / `accent.ts` / `roles.ts` / the classic CTA path), all fixed in this commit. Noted for a follow-up: `howler` is now a dead devDependency (its only consumer, `audio.ts`, is gone).
- **Alternatives:** keep Exam dormant like Classic (rejected — the Phase-5 `CategoryId` rename breaks it); delete `categoryBreakdown` with Exam (rejected — it's the step-8 match-explanation engine, kept unwired); keep `programs`/`competencies`/`skills` as a seed (rejected — orphaned once `roles.ts` is gone; a category-keyed set returns at step 8, the known zero-programs gap); fold Phase 4 + Phase 5 into one pass (rejected — destructive delete kept separate from content re-authoring). **`CLAUDE.md` and its "three role families" hard rule are deferred to the Phase-5 whole-file sweep** (`verifier.md` + `data-author` were retargeted to four-category narrative now; both get rewritten to three roles in Phase 5).
- **Affected:** ~46 files deleted or narrowed across `src/{screens,lib,data,components,scene,state,app}` + `tests/e2e`; `.claude/agents/verifier.md` + `.claude/skills/data-author/SKILL.md` (exam/classic invariants removed); `docs/rubrics/{results-screen,design-system-compliance}.md` (exam-dashboard examples retargeted); the seven spec docs via `doc-steward` (`DATA_MODEL`/`PRD`/`ARCHITECTURE`/`DESIGN_SYSTEM`/`ROADMAP`/`README`/`HARNESS`); `STATUS.md` ticked. Recovery tag `archive/pre-narrative-only`. **Next: Phase 5 — collapse the four categories to ARM's three roles.**

### D-026 — Realignment Phase 3: re-point the harness gates at the live §17 invariants
- **Decision:** Rewrite the harness evaluation layer to grade the live four-category product instead of the abandoned conveyor scene. (1) `verifier.md` + the `data-author` skill now **headline the §17 flow invariants** (narrative 6 intro MC + 7 scenes × 4 one-per-category, `expectedCategoryMax {11,11,11,11}`; exam 30 statements at 8/7/7/8, `expectedCategoryMax {operate:11, repair:8, program:10, plan:11}` — **unequal**; shared `SORT_BUCKETS`, "Kinda me" = `MAYBE_WEIGHT` 0, four `roleDetails` with 0–2 levels), mirroring `data-integrity.test.ts`; the classic 24-item / Builder 22·Innovator 27·Architect 25 / robot-parts checks are demoted to a **"documented cut, validated until the Phase-4 archival"** footnote. (2) Rubrics: **retired** `goose-game-aesthetic.md` (graded a scene that was never built) and `motion-quality.md` (folded its reduced-motion + token + engine-ownership criteria into `design-system-compliance.md`); **rewrote** `design-system-compliance.md`'s stale archetype-color + scene-namespace criteria to the four-category accent map (`categoryAccent.ts`) and kit surface discipline; **authored** `results-screen.md` (the node-map / dashboard results were ungraded). (3) **Rescoped** the `scene-motion` skill from the never-built conveyor choreography to the live motion (Landing `DrawSVG` reveal + Motion flow-step transitions + bucket-sort drag + node-map compare). (4) Reconciled `design-reviewer.md` + `HARNESS.md` to the new rubric/skill set.
- **Why:** The gates were the last load-bearing piece still describing the dead vision: `verifier` and `data-author` headlined the 24-item / three-archetype / robot-parts checks, and the `design-reviewer` would have graded live screens against a Goose-game scene that doesn't exist. Re-pointing them at the §17 invariants and authoring a results-screen rubric makes the harness grade the real product, and unblocks a graded `/design-review` on the upcoming high-fidelity results screen (step 8).
- **Verified:** a 2-skeptic adversarial **Workflow** (invariant-fidelity: every number in `verifier`/`data-author` cross-checked against `data-integrity.test.ts` + the flow data; harness-coherence: no live file still grades against the deleted rubrics, the folded motion section agrees with the rescoped `scene-motion`, the new rubric maps to real surfaces, the accent map matches `categoryAccent.ts`). **Zero findings.** Fixed one loose label ("2 education screeners" → "2 background screeners (education + experience)"). Gates green: lint, typecheck, 99 unit.
- **Alternatives:** keep the classic checks as the headline with §17 appended (rejected — §17 is the live product, it leads); delete the classic checks from the gate docs now (rejected — the classic data still exists and `data-integrity` still validates it until Phase 4; the gate doc should describe what runs); archive the retired rubrics in-tree rather than delete (rejected — a present rubric is loaded and graded against; deletion + git history + this entry is the clean record).
- **Affected:** `.claude/agents/verifier.md`, `.claude/skills/data-author/SKILL.md`, `.claude/skills/scene-motion/SKILL.md`, `.claude/agents/design-reviewer.md`, `docs/rubrics/design-system-compliance.md`, **new** `docs/rubrics/results-screen.md`, **deleted** `docs/rubrics/goose-game-aesthetic.md` + `docs/rubrics/motion-quality.md`, `docs/knowledge/HARNESS.md`; `STATUS.md` ticked. **Phases 2–3 of the realignment sweep are done; Phase 4 (the destructive archive+delete of the dormant classic code) is paused for user go-ahead.**

### D-025 — Realignment Phase 2: re-center the spec set on the narrative quiz (documented-cut convention)
- **Decision:** Rewrite every spec doc so a fresh reader meets the **live narrative quiz** first, and demote the abandoned conveyor/robot/three-archetype/Goose-game vision to a clearly-marked **"documented cut"** (the same treatment `ARCHITECTURE.md` §9's 3D path gets: kept for the record, no longer the plan). Touched `PRD`, `DATA_MODEL`, `DESIGN_SYSTEM`, `ARCHITECTURE`, `ROADMAP`, `CLAUDE.md`, `README`, a light reconciliation of `CONTEXT_BRIEF`, and as-built notes folded into the three V3 reference docs. **`DATA_MODEL.md` §17 is promoted to the primary model via status banners + a lead pointer, NOT renumbered** (renumbering would ripple through every `§`-reference in the docs and the ported `/revise-doc`+`doc-steward`, for no benefit). The four-category model leads; §1–§14 are the documented-cut classic pipeline. Driven by the `/revise-doc` discipline (canonical edit → `doc-steward` reconcile → re-check §17 invariants → log).
- **Why:** The product pivoted (D-016–D-023) but the scaffolding lagged: the PRD, roadmap, design tokens, and harness still described a factory-floor conveyor scene that was never built (`REALIGNMENT.md` §2). Re-centering the docs is the reversible half of the sweep; it unblocks the high-fidelity narrative results screen (step 8) by making a new reader meet the real product. Chose the "documented cut" convention over deletion so the decision history that makes the pivot legible (the portfolio case study's spine) survives.
- **Verified:** a 4-skeptic adversarial **Workflow** (residual-as-current / code-contradiction / voice-and-invention / completeness-critic) + the `doc-steward` reconciliation pass. The panel caught a pre-existing falsehood (`DATA_MODEL §17` said `defaultFlowId = 'classic'`; live is `'narrative'`), PRD §3 Goals + §9/§11/§14 residuals, and several DESIGN_SYSTEM live-table residuals (archetype accent, conveyor container, pedestal) — all fixed. Zero factual invention found. Em dashes in added flowing prose were converted to commas/colons (the project voice rule); structural label-separators kept to match the docs' house style.
- **Held for later phases (intentionally NOT changed):** the "exactly three role families" hard rule + D-017 carve-out and the `/src/scene` reference in `CLAUDE.md` stay (they're still true until Phase 4 archives the classic code). The parent `Capstone/CLAUDE.md` is out of scope (Cowork's domain).
- **Alternatives:** delete the conveyor material outright (rejected — loses the legible pivot history; deletion is Phase 4's job for *code*, but the docs keep the record); renumber `DATA_MODEL` so §17 leads (rejected — large cross-ref blast radius, the handoff flagged it); fold Phase 2 + Phase 3 into one commit (rejected — kept reversible doc work separate from the harness-gate rewrite).
- **Affected:** all six `docs/*.md` + `CLAUDE.md` + `README.md` + three `docs/reference/` V3 docs; `STATUS.md` ticked. **Gates green: lint, typecheck, 99 unit, 7 E2E (doc-only change, code untouched).** Next: Phase 3 (gates), then pause before Phase 4.

### D-024 — Kit-align explore_floor's design tokens to the RC UI Kit (foundation pass)
- **Decision:** Snap `src/styles/globals.css` to the dashboard's kit palette. Rename `arm-yellow`→`arm-gold` (value `#ffb81c` kept), propagated to all 7 consumers; revalue `arm-orange` `#f56a00`→`#bf5309` (AA-safe); keep `arm-teal #117289`; keep `arm-blue #38a5ee` as a **TEMP** holdout. Swap `text-default` navy-slate `#2d3a4a`→charcoal `#262626`, `border-default` `#0000001f`→opaque `#e0e0e0`, the Material triple-shadows→the kit's two soft tiers, `radius-sm` 4→6. Token **names kept** (only `arm-yellow` renamed) so live consumers don't churn; the live UI stays green.
- **Why:** The realignment unifies the prototypes on one kit-aligned design system (`REALIGNMENT.md` Rec B); the quiz was the lone holdout on the pre-kit archetype palette. Aligning now (vs. waiting for the shared `rc-design-system` package) puts the upcoming high-fidelity results screen on-kit from its first commit with no rework — subscribe-vs-hand-edit is the same alignment.
- **Verified:** Adversarial design-review (7 live screens, regression-free — the rename orphaned no fill) + WCAG/integrity check: zero dangling token refs; the orange revalue **fixed a pre-existing AA failure** (repair accent + FitNote heads-up: 3.02:1→4.71:1); charcoal ink raised body contrast (→15.1:1). `arm-blue` text stays AA-failing (2.7:1) — pre-existing, deferred.
- **Deferred:** the four category accents (`categoryAccent.ts`) reconciled with the kit's restrained teal-led voice + the `arm-blue` retone (step 8, the results redesign); the `scene-*` playful tokens removed with the classic-conveyor archival; the comprehensive `DESIGN_SYSTEM`/`PRD`/etc. palette rewrite via `/revise-doc` (Phase 2). The shared `rc-design-system` package is deferred (user call — kit-align in-repo now).
- **Alternatives:** Stand up the shared package now (deferred — cross-repo, larger); delete `arm-blue` per `REALIGNMENT.md` Appendix A (rejected — the live `program` accent + links need it until step 8).
- **Affected:** `src/styles/globals.css`; the 7 consumers (`Button`/`ProgressBar`/`DropZone`/`SegmentedControl`/`categoryAccent`/`Results/exam/CategoryBars`/`colorSchemes`); `DESIGN_SYSTEM.md` §3.1–§3.3. **Gates green: lint, typecheck, 99 unit.**

## 2026-06-22

### D-023 — Narrative intro questions (education + salary) nudge the category match
- **Decision:** The narrative's intro screeners now carry category weights on the same **tier ladder** the exam uses (D-019), so the narrative no longer scores on the seven scenes alone. **Education** (Q1 "going to college?" + Q2 "how long?") and **salary** (Q3) each add **one point** to the role(s) at the answer's appetite level: **level 0** (no college / $40k) → operate (Operator); **level 1** (1-2 years / $60k) → repair (Technician); **level 2** (4+ years / $80k+) → program + plan (Specialist + Integrator). Education is split across Q1/Q2 — `n-q1-no` carries the point on the "No" branch, otherwise Q2 does. **Q0 (experience) stays unscored** (the routing it will drive is parked), and **Q2 "Whatever" stays unscored** (noncommittal). Unlike the exam, salary scores here too. `expectedCategoryMax` moves from `{9,9,9,9}` to `{11,11,11,11}` (intro adds +2 to every category: operate via Q1+Q3, repair/program/plan via Q2+Q3). Tags only — IDs, labels, prompts, and the Q1 branch are unchanged. This is the second half of the V3 narrative pass; the first was the language update (Q0 added, Q4/Q5 one-per-category, scene rewrites, two `??` choices settled).
- **Why:** Testing found the two instruments disagree by construction — the narrative scored purely on the scenes and skewed people low (Operator in V2) while the exam's intro screeners nudge the match and landed people higher. Giving the narrative the same nudge closes the gap so the study comparison isn't confounded by the scoring model. It pairs with the results-screen "starting rung, not a verdict" reframing: tagging low appetite toward Operator is intended, and the reframing keeps that honest. The scenes still carry most of the weight (7 of 11), so the intro tilts the result, it doesn't decide it.
- **Alternatives:** Keep the narrative intro unscored (rejected — that's the confound this fixes); score Q2 "Whatever" at its fit-line level 1 → repair (rejected — Caelan chose to leave a noncommittal answer out of the score; it still feeds the fit line); leave salary fit-line-only like the exam (rejected — the team let salary count on the narrative); distinguish Specialist vs Integrator at level 2 (kept tied — both get one point, mirroring the exam, so the scenes break the tie).
- **Affected:** `src/data/flows/narrativeFlow.ts` (Q1/Q2/Q3 `categories` + `expectedCategoryMax` → 11), `data-integrity.test.ts` (recomputes + confirms the max — no edit), `tests/e2e/narrative.spec.ts` (re-verified — program stays top, percentages read from the engine — no edit needed), `docs/reference/Narrative Quiz Structure Content Spec.md`, `STATUS.md`, this entry. **Gates green: lint, typecheck, 99 unit, 7 E2E.**

## 2026-06-11

### D-022 — Role select arms on the switcher like the flows; the CTA launches it
- **Decision:** The "Role select" tab (relabeled **Select**) no longer navigates straight to `/select` on tap. It arms like the study conditions: tap the segment, then the yellow CTA (relabeled **"Select the role"**, with its own landing description) routes to `/select`. The armed value is `LandingConditionId = FlowId | 'select'` on the store's `flowId` slot, so it survives "Start over" like the flows do; `activeFlow()`/`useFlow()` fall back to the default flow defensively (the select CTA never starts a session, so flow actions can't run under it). Landing copy for the armed state lives in `data/roleSelect.ts` (`roleSelectLanding`).
- **Why:** Caelan wants one consistent researcher rhythm across all three tabs — pick a condition, hand over, participant presses the one yellow button. The straight-navigate tab broke that pattern. This supersedes the D-021 alternative that rejected widening (`FlowId` widening turned out to be two guarded fallbacks, not a parallel field).
- **Affected:** `types.ts` (`LandingConditionId`), `roleSelect.ts` (+`roleSelectLanding`), `sessionStore.ts`, `useFlow.ts`, `Landing.tsx`, `role-select.spec.ts`.

### D-021 — Classic leaves the landing switcher; the role-select comparator takes its slot; narrative becomes the default
- **Decision:** The landing condition switcher reads **Narrative / Exam / Role select**. The "Role select" tab navigates straight to `/select` (the "skip the quiz" comparator for the industry-professional arm) — it's a route, not a flow: tapping it doesn't arm a condition for the CTA. **Classic is dormant:** all code, data, and registration stay (in `flows/index.ts`, `flowList`, the §17 integrity tests, and the `/sort`/`/build` routes), but it has no UI entry. `defaultFlowId` → `narrative` so a fresh load can't silently start classic. The classic E2E specs reach it via a dev-only `globalThis.__sessionStore` handle in `sessionStore.ts` (stripped from production builds).
- **Why:** The study conditions are now narrative vs exam vs straight role-pick; classic isn't a condition in this round but the team wants it kept "just in case." Keeping it registered keeps its data validated; removing only the UI entry is the smallest cut.
- **Alternatives:** Keep classic as a fourth tab (rejected — not a study condition, clutters the researcher control); delete classic (rejected — explicit keep); persist a select "condition" in the store so the CTA starts it (rejected — needs FlowId widening or a parallel field for no protocol benefit).
- **Affected:** `Landing.tsx`, `flows/index.ts` (`defaultFlowId` only), `sessionStore.ts` (dev handle), classic E2E specs + `tests/e2e/helpers.ts`, `role-select.spec.ts`.

## 2026-06-08

### D-020 — Screeners drive an always-on education/pay fit line on results
- **Decision:** The initial screener questions also produce a results **"fit check"** (a small block next to the role in focus). Each role carries an `educationLevel` and `payLevel` (0 = HS / ~$40k … 2 = Bachelor's+ / $105k+; Specialist and Integrator both land at edu level 2 per the team). `deriveScreenerProfile` reads the user's appetite (0/1/2) off the flow's screener answers — exam Q1 directly; narrative Q1+Q2 ("going to college?" + "how long?") for education, narrative Q3 for pay — and `screenerFitLines` compares it to the role's ladder, returning an **always-shown** line per axis: a green check when the user's appetite meets or exceeds the role, an amber heads-up when the role asks for more school or pays a tier below their target. Education shows in both flows; pay is narrative-only (the exam asks no salary question). Shown next to the top match (exam dashboard) / the centered role (narrative node map). Levels + copy are data (`src/data/flows/screeners.ts` + the `roleDetails` ladders); the comparison is a pure lib (`lib/screenerFit.ts`).
- **Why:** The team wants the screeners reflected in results even when interests point elsewhere — "you map to Integrator the most, but you said you don't want school, and we should let people know." This reconciles their two framings: the **category mapping (D-019) is the score nudge**; the **0/1/2 level is the fit read**. Always-on (their call) so a clean fit gets affirmed too, not only mismatches.
- **Alternatives:** Caveat-only with no score nudge (rejected — they chose "also nudge", D-019); show only on a mismatch (rejected — they chose always-on); a 4th education level so Integrator reads as more school than Specialist (rejected — they confirmed both at level 2); pin the narrative fit to the top match rather than the swapped role (rejected — following the centered role keeps it coherent with the heading).
- **Affected:** `src/data/types.ts` (RoleDetail `educationLevel`/`payLevel`), `roleDetails.ts` (levels), `src/data/flows/screeners.ts` (new — levels + copy), `lib/screenerFit.ts` (new) + lib barrel, `Results/category/FitNote.tsx` (new), `ExamResults` + `CategoryResults` (wire), `screenerFit.test.ts` + data-integrity (level range) + narrative/exam e2e, `DATA_MODEL.md` §17, this entry. **Gates: lint, typecheck, 99 unit, 6 E2E green.**

### D-019 — Exam screener questions (Q1/Q2) nudge the category match
- **Decision:** The exam's two background screeners now carry category weights on a **tier ladder** (the team's recovered mapping, then refined): "Are you pursuing higher education?" and "Do you have prior experience?" each map **No → Operator** (operate, entry tier 0); **Maybe / Yes → Specialist + Integrator** (program + plan, advanced tier 2). The middle "Maybe" counts as **full appetite, same as Yes** (level 2) — the question reads as "not planning" vs "open to it", so being open to school/experience leans advanced. Q3 maps its four options left-to-right onto operate/repair/program/plan. The exam intro questions now **nudge the match**; `expectedCategoryMax` becomes `{operate 11, repair 8, program 10, plan 11}` (Q1/Q2 reach operate/program/plan; Q3 all four). The **salary** question stays out of the score — fit-line only (D-020). One tier level (0/1/2) drives both the score nudge and the fit line. Hardened the MC transition in the same change (a fast click could land on the exiting question's lingering button and corrupt the answer — see L-007) so scored first-step answers are reliable.
- **Why:** The team recovered the rationale and confirmed it: these questions gauge how much school/experience the user is after, which aligns with the role tier ladder, so they belong in the score as a nudge. The Maybe→2 refinement is the team's call — openness to higher ed should count toward the roles that need it, not split toward the middle tier. Wiring it surfaced (and forced a fix of) the latent MC-transition race.
- **Alternatives:** Keep them unscored background (rejected — the team wants them counted); keep the first-pass categorical split (No→Operator, Maybe→Technician+Specialist, Yes→Integrator) (rejected — the team moved Maybe up to the advanced tier); a numeric weighted-MC schema so Yes outweighs Maybe (rejected — Maybe = Yes = level 2 now, so a shared category set is simpler than per-answer weights); nudge the match with salary too (rejected — the team kept salary fit-line-only; the match stays interest-driven + the school/experience nudge). The **narrative** intro screeners stay unmapped for scoring (that half of the D-017 open item is still open) but feed the fit line via their answers.
- **Affected:** `src/data/flows/examFlow.ts` (e-q1/e-q2 categories + `expectedCategoryMax`), `src/data/flows/screeners.ts` (e-q1 Maybe level 2), `src/screens/Flow/MCQuestion.tsx` (answer lock), `screenerFit.test.ts` + `data-integrity.test.ts` (max), `DATA_MODEL.md` §17 open item, this entry.

### D-018 — Narrative scenes are a per-choice 3-bucket sort (not a single pick); middle bucket reads "Kinda me" in both flows
- **Decision:** Each of the 7 narrative **scenes** now presents its 4 choices and the user sorts **each one** into the three buckets — **That's me / Kinda me / Not me** — one card at a time, the exam's `StatementSortView` mechanic with the scene's story framing on top. This replaces the D-017 "drag your one pick into a zone" model. The middle bucket is relabeled from "Maybe" to **"Kinda me"** across **both** study flows (narrative + exam) via one shared `SORT_BUCKETS` constant (`src/data/flows/buckets.ts`); its `BucketId` stays `'maybe'`, so scoring is unchanged — "Kinda me" still scores `MAYBE_WEIGHT` (**0** today, tunable). Scene choices flow into the existing `statementBuckets` map keyed by `SceneChoice.id`, so one scene can now credit several categories or none. The sort UI is extracted into a shared `BucketSort` component used by both the exam statement sort and each scene.
- **Why:** The team clarified the FigJam board's intent: the narrative was always meant to mirror the other quizzes' sort structure (judge each option), not pick a single winner — D-017 misread the board. Keeping one bucket vocabulary across both conditions removes a wording confound from a study that's specifically comparing **question structure**. "Kinda me" is the team's preferred phrasing for the middle option. Scoring stays at 0 because the existing deliberate decision (a middle option that scores as a no, pending tuning) is unchanged; only the label moved. Reusing `statementBuckets` + `bucketWeight` means the change is mostly UI — the scoring/normalization path and per-category max are untouched (`expectedCategoryMax` still {9,9,9,9}).
- **Alternatives:** Keep the single-pick scene (rejected — contradicts the team's clarified intent and breaks structural parallel with the exam); show all 4 choices at once as a Likert grid (rejected — the user chose one-card-at-a-time to match the exam exactly); give "Kinda me" partial credit, e.g. 0.5 (rejected — the user kept it at 0, consistent with the prior decision and identical scoring across flows; flip `MAYBE_WEIGHT` later if wanted); relabel only the narrative's middle bucket (rejected — the user chose both flows for study consistency); store scene choices in `answers` like the old pick (rejected — buckets belong in `statementBuckets`; `answers` now holds only MC picks + branching).
- **Affected:** `src/data/flows/buckets.ts` (new `SORT_BUCKETS`), `src/data/flows/examFlow.ts` (uses it), `src/data/types.ts` (`SceneStep` doc), `src/screens/Flow/BucketSort.tsx` (new shared sort), `StatementSortView.tsx` (thin wrapper), `SceneSortView.tsx` (new, replaces `SceneStepView.tsx`), `FlowRunner.tsx` (scene `onDone` advances/completes), `src/lib/categoryScoring.ts` + `categoryBreakdown.ts` (scene case reads buckets), unit tests (scene scoring/breakdown), `data-integrity.test.ts` ("Kinda me" lock), rewritten `narrative.spec`, `exam.spec` ("Kinda me" check), `DATA_MODEL.md` §17, `PRD.md`, the content spec's as-built notes, this entry. **Gates green: lint, typecheck, 89 unit, 6 E2E.**

## 2026-06-07

### D-017 — Question-structure study: three selectable flows, four categories, per-flow results (supersedes the §16 A/B language test for the live study)
- **Decision:** The first user test compares which **question structure** is most engaging and most trusted, not which language treatment. Three selectable **flows** ship on one build (`DATA_MODEL.md` §17): **Classic** (the Phase 1 interest sort, wrapping set A by reference — pipeline untouched), **Narrative** (5 intro MC with a Q1 branch, then 7 day-in-the-life scenes you drag a pick into), and **Exam** (2 background MC + 1 mapped MC, then a 30-statement sort into three buckets). The two new flows score **four RC.org categories** (Operate/Repair/Program/Plan → Operator/Technician/Specialist/Integrator), a model **parallel to** the three archetypes (no change to `ArchetypeId`/`RoleId`/`ARCHETYPE_TO_ROLE`). **Each new flow has its own results presentation** (the study compares these too): Narrative → an **Obsidian-style node graph** (top match centered, the other three behind it, tap to swap in; job titles branch off the front → role sheet — rebuilt from an initial concentric-rings version that read as "funky"); Exam → a **dashboard** (robot anchor + four category bars + "why you scored that way" provenance + "your roles"). Both share the role detail sheet (RC.org content + four-axis fit radar). A researcher-facing three-way segmented control on Landing switches flows; `flowId` lives next to session state so the condition survives `reset()` (same mechanism as D-016's `questionSetId`). The robot build + build beat are **skipped** in both new flows this iteration (the exam dashboard shows a static `RobotPlaceholder` as an anchor only, tinted by the top category). The statement sort's **`maybe` bucket scores 0** via a tunable `MAYBE_WEIGHT` constant. The §16 set B placeholder + `question-set-b` spec are retired (superseded by the flow switcher).
- **Why:** Research need shifted from language to structure — the team designed two structurally different instruments on the FigJam board ("Narrative Quiz Structure") plus a shared light-fidelity results wireframe. The user chose: keep the old flow as a third selectable condition; skip the robot for now to keep presentation minimal (focus on the questions), leaving re-enabling easy; three buckets for the sort (a prior study's participants asked for a "maybe") but score maybe as a no for now, tunable later; scene picks are drag-to-zone. Building flows as a discriminated union (classic wraps the existing QuestionSet by reference) keeps the Phase 1 pipeline at zero churn while the new flows declare ordered steps.
- **Alternatives:** Extend the §16 QuestionSet to carry structure (rejected — the classic scoring/robot/results pipeline keys off the 24-item shape; forcing four categories + steps into it would ripple through scoring.ts and every classic test); widen `ArchetypeId` to four (rejected — same ripple; the category world is cleanly separate and the new results read from `roleDetails`, not `roles.ts`); replace the old flow entirely (rejected — the user wants it as a comparison condition); keep robot part-mappings for the new flows (rejected — deferred with the rest of the build beat).
- **Affected:** `src/data/types.ts` (CategoryId/Flow/FlowStep/CategoryResult/RoleDetail), `src/data/flows/` (narrative/exam/classic + registry), `src/data/roleDetails.ts`, `src/lib/categoryScoring.ts` + `nodeLayout.ts` (+ tests), `src/state/sessionStore.ts` (flowId, category actions) + `useFlow`/`useQuestionSet`, generalized `DragSortCard`/`DropZone`, `src/screens/Flow/` (runner + MC/scene/statement views), `src/screens/Results/` (dispatcher + `ClassicResults` + `category/`), router `/flow`, Landing switcher, `data-integrity.test.ts` (§17 per-flow), new `narrative`/`exam` e2e, `DATA_MODEL.md` §16–§17, `CLAUDE.md` hard-rule carve-out, this entry.
- **Open item (revisit with the team):** the background questions (narrative Q1–Q3, exam Q1–Q2) are meant to map to *something* — they shape what the user is presented with — but the rationale is unrecovered, so they ship with empty category weights (`MCChoice.categories: []`). The schema is ready; adding weights later is a data edit only. Also pending: the two `??`-flagged narrative choices ("IT club" scene 4, "Writing code" scene 6) ship as-authored; the on-canvas title scatter from wireframe 1 was simplified to a tray (collision with 3-5 wide titles) and wants a polish pass.

## 2026-06-04

### D-016 — A/B question-set instrument inserted between Phase 1 and Phase 2
- **Decision:** The first user test compares two language treatments (formal/exam-like vs playful/narrativized), so content ships as two swappable **question sets** (`DATA_MODEL.md` §16): each set owns its 24 items (own ids/labels/weights/robot mappings) plus the landing, sort, round, and results copy; roles/competencies/skills/programs/parts stay shared. A researcher-facing segmented control on Landing switches sets; `questionSetId` lives **next to** session state in the store (not inside it) so `reset()`'s shallow merge preserves the condition between participants. Per-set **declared `expectedSums`** replace the global 22/27/25 sums invariant (recomputed-vs-declared, per set). Set B ships as a loud `[B]`-marked placeholder clone until the compiled content lands via the worksheet (`docs/knowledge/QUESTION_SET_WORKSHEET.md`) + data-author skill.
- **Why:** Research need — the test isolates language treatment, so both treatments must run on the same build with a switch the researcher can flip between sessions without re-setup. Building the registry to hold *fully independent* sets (not label overlays) covers both rewordings and structurally different question sets, which the team hasn't finished compiling. User chose full-experience scope (cards + flow + results copy), independent structure, and a visible landing toggle.
- **Alternatives:** Label-overlay map per set (rejected — locks out different items/weights); env/build-time switch or URL param (rejected — researcher needs an in-app flip); two deployed builds (rejected — heavier setup, drifts).
- **Affected:** `src/data/types.ts` (QuestionSet/SortCopy/LandingCopy/ResultsCopy), `src/data/questionSets/`, `src/state/sessionStore.ts` + `useQuestionSet`, `src/components/SegmentedControl.tsx`, Landing/Sort/Results/FourPartRead, `data-integrity.test.ts` (per-set `describe.each`), new `tests/e2e/question-set-b.spec.ts`, `DATA_MODEL.md` §13–§16, `PRD.md` §8/§14, data-author skill invariants. **Known boundary:** `Build.tsx` copy, the Results empty state, and chrome strings are intentionally NOT set-owned in this pass. Also fixed: `playwright.config.ts` now follows vite's port move to 5174.

## 2026-05-30

### D-015 — Phase 1 sort interaction is drag + tap only (no arrow-key/Enter mechanic)
- **Decision:** The Phase 1 Sort screen is sorted by **dragging the card into a bin or tapping a bin**. The bespoke "arrow keys to focus a bin, Enter to choose" mechanic (built then removed) is dropped. The two bins remain native `<button>`s, so basic keyboard operability (Tab to a bin, Enter/Space to activate) still exists for free — what's gone is the custom arrow-navigation layer.
- **Why:** User call during the Slice 1 checkpoint — the arrow/Enter layer added UI state and a focus model that didn't fit the intended drag-first interaction, and it foreshadows the Phase 2 conveyor where sorting is purely drag-off-the-belt. Native button semantics keep the experience keyboard-reachable without the bespoke layer.
- **Alternatives:** Keep the arrow/Enter mechanic (rejected per user); remove keyboard entirely incl. button activation (rejected — needlessly drops the free native a11y).
- **Affected:** `src/screens/Sort/Sort.tsx`, `src/screens/Sort/SortBin.tsx`; `ROADMAP.md` §2.2 (keyboard bullet) + the round-beat copy example. Full keyboard-nav polish remains a Phase 3 a11y item.

### D-014 — Phase 2 sort model: user drags parts off the belt; the arm assembles from the keep bin — *superseded-in-practice (Phase-2 conveyor cut; D-025 documented-cut, D-027 strip)*
- **Decision:** Refine the Phase 2 conveyor interaction. **The user drags interest parts directly off the moving belt into one of two bins set in front of the line (downscreen in 2D): "That's me" / "Not my thing." A robot arm then lifts each kept part from the "That's me" bin and assembles it onto the robot standing behind/above the belt.** A second arm — or a trash chute — clears the "Not my thing" bin (exact treatment is a Phase 2 authoring choice, TBD). This supersedes the earlier "user controls the arm to push items to bins" framing.
- **Why:** User's articulated vision at the Slice 1 checkpoint. Dragging parts directly is more legible and direct than puppeteering an arm; reserving the arm for *assembly* gives the robot-build payoff a clear, visible cause (kept part → arm → robot). Engine ownership stays clean: Motion owns the user's drag-off-belt gesture; GSAP owns the belt, the assembling arm(s), and the part-to-robot snap.
- **Alternatives:** User-controlled sorting arm (the prior doc model; rejected — indirect, more interaction-design risk); auto-sorting with the user only watching (rejected — removes agency, the core "half-real decision" the PRD is built on).
- **Affected:** `ROADMAP.md` §3.2/§3.3, `PRD.md` §5.2/§5.3, `DESIGN_SYSTEM.md` §10.3, `ARCHITECTURE.md` §5. Phase 2 builds to this model; Phase 1 stays the plain card-sort.

## 2026-05-29

### D-013 — Tailwind v4 (CSS-first): design tokens live in `@theme`, not `tailwind.config.ts`
- **Decision:** Phase 0 scaffolds on **Tailwind v4** via the `@tailwindcss/vite` plugin. The design-token source of truth is the `@theme` block in `src/styles/globals.css`; there is no `tailwind.config.ts` and no `postcss.config.js`/`autoprefixer`. Token **names are unchanged** (`arm-yellow`, `space-4`, `text-h1`, …) so the Figma-variable mirror and the tokens-not-literals rule are unaffected. Easing curves are mirrored into `@theme` (`ease-soft`/`ease-snap`); motion **durations** + the spring stay in `/src/lib/motion.ts` (v4 has no duration token namespace).
- **Why:** The user chose v4 explicitly during Phase 0 planning. v4 is the current major and a fresh scaffold pulls it by default; it moves tokens into CSS-first `@theme`, which is the idiomatic v4 model. Keeping token names identical preserves every downstream convention.
- **Alternatives:** Tailwind v3 with `tailwind.config.ts` (the original doc assumption; rejected per the user's call — would pin to the prior major); v4 in legacy `@config` compat mode loading a `tailwind.config.ts` (rejected — fights the v4 grain for no benefit once names are preserved).
- **Affected:** `src/styles/globals.css` (the `@theme` block), `package.json` (`tailwindcss@4` + `@tailwindcss/vite`, no postcss/autoprefixer), `vite.config.ts`. Docs reconciled to point at `@theme`: `CLAUDE.md`, `docs/ARCHITECTURE.md` §1/§3/§7, `docs/DESIGN_SYSTEM.md` §2/§8, `docs/ROADMAP.md` §1.2, `docs/rubrics/design-system-compliance.md`.

### D-012 — Don't ship a project `.mcp.json`; configure MCP servers globally (supersedes D-009)
- **Decision:** Remove the committed `.mcp.json`. MCP servers (`figma` / `playwright` / `firecrawl`) are expected to be configured globally (`claude mcp add` or an existing global setup). `.claude/settings.json` keeps `enabledMcpjsonServers` as a name-whitelist so a teammate with no global config can drop in their own local `.mcp.json` and have it auto-enable.
- **Why:** A project `.mcp.json` whose server names collide with the owner's global config caused duplicate-server issues in Claude Code. The app never depends on the file (it's dev-time tooling, not runtime), so removing it is the clean fix; the whitelist preserves the per-clone fallback.
- **Alternatives:** Keep `.mcp.json` and rename project servers to avoid collisions (rejected — churns the docs + still double-registers tools); `git update-index --skip-worktree` to keep it tracked but locally deleted (rejected — fragile, hides the file's absence from teammates).
- **Affected:** `.mcp.json` (deleted), `README.md`, `docs/ARCHITECTURE.md`, `docs/knowledge/HARNESS.md`. Historical refs in `STATUS.md` + the 2026-05-29 session note are left as-dated.

### D-011 — Defer the live RC.org capture until aesthetic-rubric authoring — *superseded-in-practice (its target `goose-game-aesthetic.md` was retired in D-026)*
- **Decision:** Don't scrape/screenshot RoboticsCareer.org yet. Capture it (Firecrawl + Playwright) when we author the aesthetic/design-system rubrics, ~Phase 1.
- **Why:** RC.org's brand tokens are already in `DESIGN_SYSTEM.md` verbatim; a live capture mainly grounds the *aesthetic* rubric and the before/after narrative, which we don't need until visual work starts.
- **Alternatives:** Capture now (rejected — premature); skip entirely (rejected — loses before/after + rubric grounding).
- **Affected:** `docs/rubrics/goose-game-aesthetic.md` (carries a TODO to add RC.org reference stills).

### D-010 — Motion skills via the free MIT community package (no Motion+)
- **Decision:** Install Motion (Framer) agent skills from `C-Jeril/framer-motion-skills` (MIT). Do **not** use `npx motion-ai`.
- **Why:** The official Motion AI Kit requires a paid Motion+ subscription, which we don't have. The community skills are free, open-source, Claude-Code-compatible, and cover our surface (components, variants, gestures, layout/shared transitions).
- **Alternatives:** `npx motion-ai` (rejected — paywalled); no Motion skills (rejected — leaves a gap vs GSAP).
- **Note:** It references legacy `framer-motion` imports; we use `motion/react`. Identical API; our `scene-motion` skill is authoritative for conventions.
- **Affected:** `README.md`, `.claude/skills/scene-motion`, toolchain setup.

### D-009 — Declare project MCP servers in `.mcp.json` for onboarding — *superseded by D-012*
- **Decision:** Add `figma` (remote HTTP), `playwright`, `firecrawl` to `.mcp.json`.
- **Why:** A teammate cloning the repo has none of the owner's global plugins; project-scoped servers get them productive immediately.
- **Alternatives:** Rely on each person installing plugins (rejected — slow onboarding, undocumented).
- **Affected:** `.mcp.json`, `.env.example`, `README.md`.

### D-008 — Knowledge lives in-repo; mirror durable facts to built-in memory
- **Decision:** `docs/knowledge/` is the version-controlled source of truth; a few durable agent-preference facts are mirrored into Claude built-in memory.
- **Why:** In-repo knowledge is reviewable in diffs, travels to the ARM handoff, and seeds the portfolio case study. Built-in memory adds auto-recall without re-reading files.
- **Alternatives:** Built-in memory only (rejected — invisible to handoff/portfolio).
- **Affected:** `docs/knowledge/*`, built-in memory.

### D-007 — Build a bespoke harness; don't install the BilLogic plugin
- **Decision:** Author our own harness. Borrow only the BilLogic plugin's five-layer frame and its rubric YAML+prose schema. Cut its heavy ritual (SHA-256 rule-consent, `ideations/`/`preferences/` taxonomy, `hd-config.md`, auto-generated reports).
- **Why:** The plugin is more prescriptive than we want; authoring our own is leaner, fits the project, and is the stronger portfolio story (a practice we built, not installed).
- **Alternatives:** Install the plugin wholesale (rejected — prescriptive, generic).
- **Affected:** `.claude/`, `docs/knowledge/`, `docs/rubrics/`.

### D-006 — Allow demo affordances despite "no undocumented steps" — *superseded-in-practice (Phase-3 demo tooling, retired with the narrative pivot)*
- **Decision:** Permit a `?demo=true` pre-filled-results mode and a hidden skip-to-results link (Phase 3 tooling).
- **Why:** Demos and user-test setup are much faster with them; they're not user-facing flow steps.
- **Alternatives:** Strict adherence to the PRD screen list (rejected — friction for demos).
- **Affected:** `PRD.md` §14 (note added), `ROADMAP.md` §4.5/§4.7.

### D-005 — Remove project timeframes from the docs
- **Decision:** Strip schedule estimates (phase durations, "six weeks") from `ROADMAP.md`/`PRD.md`. Keep UX/animation timings.
- **Why:** Work ships when acceptance criteria are met, not on a fixed calendar; the dates added noise and false precision.
- **Alternatives:** Keep estimates (rejected — not how this project actually runs).
- **Affected:** `ROADMAP.md` §0, `PRD.md` §3.

### D-004 — Landing scene gets a Phase 1 GSAP `DrawSVG` reveal
- **Decision:** Move the landing-scene entrance animation earlier — a gentle `DrawSVG` reveal in Phase 1 as a low-risk test of the scene-animation approach (was "no animation yet").
- **Why:** Cheap to do, de-risks the GSAP path before Phase 2 leans on it heavily.
- **Affected:** `DESIGN_SYSTEM.md` §10.3, `ROADMAP.md` §2.1.

### D-003 — Vitest is part of the stack and `pnpm test`
- **Decision:** Name Vitest explicitly alongside Playwright (`CLAUDE.md` stack + verify); `pnpm test` = Vitest units + Playwright E2E.
- **Why:** The scoring engine and other pure functions need fast unit tests; `CLAUDE.md` had under-stated this.
- **Affected:** `CLAUDE.md`.

### D-002 — Standardize the file-size re-check threshold at ~250 lines
- **Decision:** Use ~250 lines across all docs as a soft "re-check responsibilities" signal (not a hard limit).
- **Why:** `CLAUDE.md` said ~200 and `ARCHITECTURE.md` said ~250; one number, framed as steering against drift.
- **Affected:** `CLAUDE.md`.

### D-001 — Innovator archetype max = 27; scoring normalizes per archetype
- **Decision:** The Innovator weight column sums to **27** (not the previously stated 24). Document the true sums (Builder 22 / Innovator 27 / Architect 25) and the fact that maxes need not be equal.
- **Why:** The scoring engine normalizes each archetype against its **own** max (`raw[A]/max[A]*100`), so unequal maxes don't favor any archetype. The "balanced maxes" claim was both numerically wrong and conceptually unnecessary. Chose to fix the docs, not re-tune 24 weights (it's a prototype; the dev team owns real tuning).
- **Alternatives:** Re-tune weights so Innovator = 24/25 (rejected — busywork with no scoring benefit).
- **Affected:** `DATA_MODEL.md` §3 (×2) + §15; relevant to Phase 0 `items.ts` + the Vitest sanity check.
